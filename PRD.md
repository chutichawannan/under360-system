# PRD — งานที่กำลังทำอยู่ตอนนี้

> **ไฟล์นี้คืออะไร:** สเปคของงาน **ชิ้นเดียวที่กำลังทำ** — 1 งาน 1 แผ่น
> **กติกา:** เปลี่ยนงาน = เขียนทับไฟล์นี้ทั้งแผ่น (ของเก่าย้ายไปสรุปใน `docs/UNDER360_HISTORY.md`)
> **แบ่งหน้าที่:** `CLAUDE.md` = กฎถาวร · **`PRD.md` = งานตอนนี้** · `docs/UNDER360_MASTERNOTE_v6_7.md` = ยุทธศาสตร์/backlog · `docs/UNDER360_HISTORY.md` = ประวัติ
> Claude อ่านไฟล์นี้ก่อนลงมือทุกครั้ง — **อะไรที่ไม่อยู่ในแผ่นนี้ = ไม่ทำ** (ถ้าคิดออกกลางทาง → จด backlog ใน `TODO.md`)

---

## งาน: ดูออเดอร์ล่วงหน้าจากมือถือ (PWA) + LINE push สรุปออเดอร์พรุ่งนี้
**ที่มา:** `CLAUDE_CODE_BRIEF_widget.md` (บรีฟมือถือ 26 ก.ค. 2026) ข้อ 3 + 4
**เวอร์ชัน:** u0.4.37 · **branch:** `feature/orders-upcoming`

### Goal
นัทเปิดไอคอนหน้าโฮมมือถือ → เห็นทันทีว่า **ออเดอร์พรุ่งนี้มีใครบ้าง** และวันถัดไปถึง 7 วัน โดยไม่ต้องไล่เปิด Operation Hub
และตอน 3 ทุ่ม (ก่อน deadline สั่งวัตถุดิบ) ให้ข้อมูลวิ่งมาหาเองทาง LINE

### Scope (ทำแค่นี้)
1. `pwa/orders_upcoming.html` — ไฟล์เดียวจบ อ่านอย่างเดียว
   - 7 วันข้างหน้า แยกการ์ดต่อวัน (หัวการ์ด = วันที่ + จำนวนออเดอร์ + จำนวนกล่องรวม)
   - แต่ละออเดอร์: ชื่อ · เบอร์ (กดโทรได้) · ประเภท (HP/LC/สต็อค/เซ็ต-แพค) · จำนวนกล่อง · ช่วงเวลา · ที่อยู่ · สถานะโอน · เลขออเดอร์+ยอด
   - แยก **Meal Plan (ทำสด)** ออกจาก **สต็อค/เซ็ต-แพคเกจ** ในวันเดียวกัน (คนละไลน์การผลิต)
   - default เปิดมา = **พรุ่งนี้** · แถบวันที่ด้านบนกดกระโดดได้ · ปุ่มรีเฟรช + เวลาที่ดึงข้อมูลล่าสุด
2. PWA: `pwa/manifest.webmanifest` + `pwa/sw.js` + ไอคอน 192/512 — เพิ่มลงหน้าโฮมได้ทั้ง Android/iOS
3. `api/notify-daily-orders.js` + cron 21:00 น. เวลาไทย — สรุปออเดอร์พรุ่งนี้เข้า LINE (Messaging API ของ OA)

### ไม่ทำ (out of scope)
- ปุ่มแก้ไข/ลบ/เปลี่ยนสถานะใดๆ (กันกดพลาดบนมือถือ — แก้ที่ OH เท่านั้น)
- ดึงจาก `mp_deliveries` (รอบ Meal Plan ระยะยาว) — v1 ใช้ `orders`+`order_items` อย่างเดียว
- login/สิทธิ์ผู้ใช้ · widget บนหน้าโฮม Android (KWGT/Tasker) · งานอื่นทุกอย่างใน backlog

### กฎที่ต้องไม่ผิด
- `const sb = supabase.createClient(...)` ตรงๆ (ไม่ใช่ `getSB()`) · `DEV_MODE = false`
- **sequential reads เท่านั้น — ห้าม `Promise.all()`**
- **วันที่ยึดเวลาไทยเสมอ** (`Asia/Bangkok`) เพราะนัทเปิดจากโซนเวลาอื่นได้
- service worker **ห้าม cache ข้อมูลออเดอร์** (cache แค่เปลือกหน้า) — ข้อมูลเก่าอันตรายกว่าไม่มีข้อมูล
- scope ของ SW = `/pwa/` เท่านั้น **ห้ามวางที่ root** (จะไปคุม `liff_customer.html` ที่ลูกค้าใช้จริง)
- `node scripts/check-html-js.js pwa/orders_upcoming.html` หลังแก้ทุกครั้ง

### Acceptance
- [x] เปิดหน้า → เห็นออเดอร์ 7 วัน แยกวัน/แยกไลน์การผลิต ไม่มี error ใน console
- [ ] เปิดจากไอคอนหน้าโฮม (Android Chrome) → เห็นออเดอร์พรุ่งนี้ภายใน 2 วินาที
- [ ] เปิดจากไอคอนหน้าโฮม (iOS Safari) → เหมือนกัน *(ยังไม่เคยเทส iOS เลยทั้งระบบ)*
- [ ] `/api/notify-daily-orders?dry=1` คืนข้อความสรุปถูกต้อง
- [ ] ตั้ง `LINE_CHANNEL_ACCESS_TOKEN` + `ORDERS_NOTIFY_TO` แล้ว LINE เด้งจริงตอน 21:00 น.

### บล็อกเกอร์ (นัทต้องกดเอง)
- `LINE_CHANNEL_ACCESS_TOKEN` ใน Vercel env (ค้างมาตั้งแต่ Known Issues #5)
- `ORDERS_NOTIFY_TO` = LINE userId ของนัท (หรือ groupId ของกลุ่มทีม)
- เทส iOS Safari + Android "เพิ่มลงหน้าโฮม"
