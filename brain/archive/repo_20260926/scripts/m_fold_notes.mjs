/* ยุบที่จดของห้อง m ตามคำสั่งเลขา (นัทเคาะ 10 ก.ย. 2026)

   ⚠️ ทำไมเป็นสคริปต์ ไม่ใช่ก๊อปไฟล์ทับ:
      `docs/OPEN_LOOPS.md` เป็นไฟล์กลาง ทุกห้องกำลังยุบพร้อมกันวันนี้
      ห้องฟ้า commit ทับไปแล้วเมื่อเช้า (7cc179f) — ถ้าผมก๊อปไฟล์ในเครื่องทับ
      จะลบงานเขาหายไป 140 บรรทัดแบบเงียบๆ
   → สคริปต์นี้ "แก้เฉพาะจุดของห้อง m" บนไฟล์สดที่ดึงมาจาก origin/main
      รันซ้ำได้ · ถ้าหาจุดยึดไม่เจอ = หยุด ไม่เดา */
import fs from 'fs';

const must = (c, m) => { if (!c) { console.error('❌ ' + m); process.exit(1); } };

/* ═══ ① กฎ "ห้ามแตะ web/" → ไฟล์กฎกลาง ═══
   เดิมอยู่ในโน้ตของห้อง m คนเดียว คนที่มาแก้ web/ ส่วนใหญ่ไม่เคยเปิดไฟล์นั้น */
{
  const F = 'docs/IRON_RULES.md';
  let h = fs.readFileSync(F, 'utf8');
  if (h.includes('กฎข้อ 7')) console.log('  ⏭️  IRON_RULES มีกฎข้อ 7 แล้ว');
  else {
    const A = '## 📌 บทลงโทษเชิงระบบ (ไม่ใช่การตำหนิ)';
    must(h.includes(A), 'ไม่เจอจุดแทรกใน IRON_RULES');
    h = h.replace(A, `## กฎข้อ 7 — ⛔ ของในโฟลเดอร์ \`web/\` ที่ห้ามแตะ (ย้ายมาจาก \`M_WEB_STATUS.md\` 10 ก.ย. 2026)

> ย้ายมาไว้ที่นี่เพราะเดิมอยู่ในไฟล์โน้ตของห้อง M คนเดียว —
> **คนที่จะมาแก้ \`web/\` ส่วนใหญ่ไม่เคยเปิดไฟล์นั้น**

| ของ | ทำไม |
|---|---|
| \`web/vercel.json\` → คีย์ **\`redirects\` 23 เส้น** | ลิงก์เก่าจาก Wix **อายุ 10 ปี** · หายเมื่อไหร่ = **SEO หายทันทีและกู้ไม่ได้** · **แก้ได้เฉพาะ \`rewrites\`** |
| **DNS ของโดเมน** | แตะผิด = เว็บดับทั้งเว็บ · โดเมนต่ออายุ 9 ธ.ค. 2026 (จดผ่าน Wix — **ยกเลิก Premium ได้ ห้ามแตะ Domain**) |
| **ลิงก์ที่ครัวใช้อยู่** \`/k\` \`/kq\` \`/o\` | ครัวจดลิงก์ไว้ในมือ · เพี้ยน = **ครัวหยุดงานจริง** (เกิดมาแล้ว 8 ก.ย.) · ห้ามแม้แต่เพิ่ม rewrite ทับ |
| ไฟล์ห้องอื่น | \`liff_*.html\` · \`command_center.html\` · \`scripts/finance/\` · \`web/tiang/\` |

**ก่อน–หลังแก้อะไรใน \`web/\` ให้รัน:** \`node scripts/m_watch_web.mjs\`
เช็ค Pixel · GTM · Google Ads · ปุ่มสั่งไป LIFF · ตัวนับสถิติ · **redirect ครบ 23 เส้น** · จำนวนบทความ · แพลนเมนูเหลือกี่วัน
**exit 1 = มีของหาย ห้าม push**

---

${A}`);
    fs.writeFileSync(F, h);
    console.log('  ✅ IRON_RULES: เพิ่มกฎข้อ 7 (ห้ามแตะ web/)');
  }
}

/* ═══ ② ของค้าง + ของพักไว้ → OPEN_LOOPS section ห้อง m ═══ */
{
  const F = 'docs/OPEN_LOOPS.md';
  let h = fs.readFileSync(F, 'utf8');

  /* ②.1 แถว attribution — หลักฐานมาแล้ว อัปเดตให้ตรงของจริง (ไม่ปิดเอง ตามกฎท้ายตาราง) */
  const OLD = '| **พิสูจน์ว่า "ที่มาลูกค้า" ไปถึงใบสั่งซื้อจริง** | 20 ส.ค. | ✅ นัทเทสเปิดหน้าจากลิงก์ติดแท็กแล้ว **หน้าไม่พัง** · merge ขึ้น main แล้ว · ⏳ **แต่ยังไม่เห็นแถวจริง** — `orders.source_campaign` ยังมี **0 ใบ** · เช็คซ้ำได้ `node scripts/m_watch_attribution.mjs` | **ออเดอร์จริงใบแรกจากลิงก์ติดแท็ก** — ยังไม่ปิด |';
  if (h.includes(OLD)) {
    h = h.replace(OLD, '| **พิสูจน์ว่า "ที่มาลูกค้า" ไปถึงใบสั่งซื้อจริง** | 20 ส.ค. | 🟢 **หลักฐานมาแล้ว 8 ก.ย.** — ออเดอร์ `U-1008-001` ฿4,140 ติดที่มา `fb/paid/jay2026-c2` = ตามได้ถึงชิ้นงานโฆษณาใบที่ยิง · เช็คซ้ำ `node scripts/m_watch_attribution.mjs` | **นัทรับทราบ** (M ปิดเองไม่ได้ตามกฎท้ายตาราง) |');
    console.log('  ✅ OPEN_LOOPS: อัปเดตแถว attribution (มีหลักฐานจริงแล้ว)');
  } else console.log('  ⏭️  แถว attribution อัปเดตแล้ว หรือถูกแก้ไปแล้ว');

  /* ②.2 ของพักไว้ + สถานะเว็บ */
  const ANCHOR = '> ⚠️ **ห้าม M ปิดเองสักข้อ** — ปิดได้เมื่อนัทเคาะ หรือนัทบอกว่าไม่เอา (แล้วจดเหตุผล) เท่านั้น';
  must(h.includes(ANCHOR), 'ไม่เจอท้ายตารางห้อง m');
  if (h.includes('ยุบมาจาก `M_WEB_STATUS.md`')) console.log('  ⏭️  ของพักไว้ย้ายแล้ว');
  else {
    h = h.replace(ANCHOR, ANCHOR + `

### 🅿️ ของที่พักไว้ (ยุบมาจาก \`M_WEB_STATUS.md\` · 10 ก.ย.)

| เรื่อง | สถานะ | รอใคร |
|---|---|---|
| **Kidneycare** \`web/kidneycare.html\` | นัทสั่งพัก 31 ส.ค. *"รอเฟสถัดไป"* · ปิดไว้ครบ (\`noindex\` + ไม่ผูกเส้นทาง + ปิดปุ่มส่ง) · 🔴 **เหตุผลที่ห้ามเปิด: ฟอร์มไม่ส่งข้อมูลไปไหนเลย ไม่มี fetch สักจุด** — หน้านี้ขอ ชื่อ·เบอร์·**ค่าเลือด**·ยา·ที่อยู่ + อัปรูปผลเลือด · เปิดตอนนี้ = ผู้ป่วยกรอกครบแล้วไม่มีใครได้รับ · ปลดล็อกด้วยการลบก้อน \`u360-kidney-hold\` แล้วต่อปลายทาง · บริบทเต็ม \`docs/KIDNEYCARE_BRIEF.md\` | **นัท** — เคาะ 3 ข้อก่อนเปิด: ข้อมูลไปไหน · PDPA ใครรับผิดชอบ · คุณอิ๊งรับไหวกี่เคส/สัปดาห์ |
| **\`web/v03.html\`** ร่างหน้าแรก v0.3 | ของดีถูกยกเข้า \`index.html\` หมดแล้ว · ไม่มีหน้าไหนลิงก์ไป เข้าได้ทางเดียวคือพิมพ์ URL · เก็บเป็นของอ้างอิง ไม่กินอะไร | **นัท** (จะเก็บหรือลบ) |
| **รูปปกบทความ Snack Plate** | บทความเดียวใน 63 ที่ไม่มีรูปปก · ใช้รูปสำรองของแบรนด์ไปก่อน พรีวิวลิงก์ไม่โล่ง | **พลอย** (ส่งรูป) |
| **ยืนยันธุรกิจกับ Meta** | 06 ยิงแอดได้ด้วยทางอ้อม (ก๊อปชุดที่เคยอนุมัติ) · **ถ้า Meta ปิดช่องนั้น = สร้างแอดใหม่ไม่ได้ทั้งบัญชี** · ต้องแก้ชื่อใน Meta เป็น "บริษัท อันเดอร์สามหกศูนย์ฟู๊ด จำกัด" + เลขภาษี \`0105563025674\` ให้ตรงหนังสือรับรองก่อนยื่น | **นัท** (ยื่นเอกสารเอง ที่ \`facebook.com/id/hub\`) |
| **ต่อเส้นทาง \`/staff\`** | นัทเคาะให้ทำโถงพนักงาน \`360foodbox.com/staff\` · ฝั่ง M = ต่อ rewrite ให้ห้อง u ทำหน้าได้ · ⛔ **รอบนี้ห้ามแตะฝั่งครัว** (\`/k\` \`/kq\` \`/o\` · ครัวจดลิงก์เดิมไว้ เพี้ยน = ครัวหยุดงาน) | **u** (ยังไม่ทักมาคุย) |

### 📄 สถานะเว็บ ณ 10 ก.ย. (ย้ายมาจาก \`M_WEB_STATUS.md\` — ไม่ใช่ของค้าง แต่ไม่มีชั้นไหนรับ)

\`/\` หน้าแรก · \`/mealplan\` (ปฏิทินเมนูตามวันส่ง) · \`/pack\` (แลนดิ้งแอด FB + ตะกร้าจบในเว็บ) · \`/jay\` + \`/jay-v5\` (คอร์สเจ — แลนดิ้งแอดที่วิ่งอยู่) · \`/prayer\` + \`/prayer/worship\` (หน้าสักการะ) · \`/blog\` 63 บทความ · \`/ads\` (ตัวเลขแอด — มีรหัส) · \`/kol\` (ลิงก์อินฟลูฯ) · \`/kidneycare.html\` 🅿️ ไม่มีทางเข้า

**แพลนเมนู** อ่านตามลำดับ: ① \`kitchen_data.mp_menu_plan\` (ของสด — ✅ มีแถวแล้ว) → ② \`web/mp_plan.json\` (สำรอง สร้างด้วย \`node scripts/m_build_mp_plan_json.mjs\`)
📌 **แพลนหมด 5 ต.ค.** — ใกล้ถึงให้ขอตารางรอบใหม่จากห้องฟ้า
🐕 **ตัวเฝ้า** \`node scripts/m_watch_web.mjs\` — รันก่อน/หลังแก้เว็บทุกครั้ง · exit 1 = มีของหาย`);
    fs.writeFileSync(F, h);
    console.log('  ✅ OPEN_LOOPS: ย้ายของพักไว้ + สถานะเว็บ เข้า section ห้อง m');
  }
}

console.log('\n✅ ยุบเสร็จ — ไม่ได้แตะ section ของห้องอื่นสักบรรทัด');
