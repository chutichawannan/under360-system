# 🎓 บทเรียนถาวร — ของที่แลกมาด้วยความเจ็บ

> สร้าง 15 ส.ค. 2026 · **นัทสะกิดเอง:** *"ถ้าความสำคัญถูกนายเกือบลบไปอีกเหมือนของฟ้า คือไม่ได้เลยนะ"*
> **ที่มา:** pm ย้ายเอกสารเข้า archive แล้วนัทถามว่าอ่านครบจริงไหม → ยอมรับว่าอ่านแค่หัวไฟล์ → **สแกนเนื้อในจริงทุกไฟล์** แล้วพบว่ามีบทเรียน 20+ ข้อฝังอยู่ในไฟล์ที่กำลังจะไม่มีใครเปิด
> 🔴 **กฎของไฟล์นี้: บทเรียนต้องอยู่ที่นี่ ไม่ใช่ในไฟล์ที่มีวันที่** · ไฟล์ที่มีวันที่จะถูก archive เสมอ แต่ไฟล์นี้ไม่มีวันหมดอายุ

---

## 🖥️ เครื่องมือโกหกเราได้ โดยไม่มีอะไรแดง

> แพทเทิร์นที่เจอซ้ำที่สุดในโปรเจคนี้ — **"ได้ผลลัพธ์ที่ดูปกติ แต่ผิด"**

| # | บทเรียน | เคยทำให้เกิดอะไร |
|---|---|---|
| 1 | **`.select()` ไม่มี `.limit()` → Supabase คืนแค่ 1,000 แถว** | ครัวมองไม่เห็นออเดอร์วันนี้ (ได้แต่ปี 2024) · หน้าลูกค้าโชว์ยอดขาดหาย |
| 2 | **`.limit(50)` ที่ไม่มีใครสังเกต** | บทความ 11 ตัวหายจากหน้ารายการเว็บ |
| 3 | **output ว่าง ≠ ไม่มีของ** — ต้องเช็ค exit code/stderr ก่อน | `git ls-tree` ที่ git ปฏิเสธ คืน stdout ว่าง → เข้าใจว่างานหายทั้งโฟลเดอร์ |
| 4 | **pipe บัง exit code** — `cmd \| tail` คืน exit ของ `tail` | เคย push ไฟล์ครัวที่ syntax พังขึ้น main |
| 5 | **`DELETE` ผ่าน anon คืน 2xx แต่ไม่ลบจริง** | ต้องใช้ `PATCH status=cancelled` และ **สแกนซ้ำหลังทำเสมอ** |
| 6 | **`stock_total != null` เป็นเงื่อนไข** | เมนูที่ตั้ง "ไม่จำกัด" ครัวกรอกผลิตไปเท่าไหร่ก็ไม่มีผล |
| 7 | **`curl` กับ URL ภาษาไทยบน Windows ได้ `???`** | ข้อมูลเพี้ยนเงียบ |
| 8 | **`node -e` กับข้อความไทยที่มี backtick → shell กินทิ้ง** | ข้อความบนบอร์ดหายเป็นช่วงๆ แบบเงียบ → **เขียนเป็นไฟล์ `.mjs` แล้วรันเสมอ** |

**→ กฎรวบยอด: ก่อนบอกว่า "ใช้ได้แล้ว" ต้อง query นับแถวจริง ไม่ใช่ดูว่าไม่มี error**

---

## 🗄️ ข้อมูลในระบบเราเอง ที่ต้องรู้ก่อนไปแตะ

| # | บทเรียน | ทำไมสำคัญ |
|---|---|---|
| 9 | 🔴 **`order_items` กับ `mp_deliveries` ไม่ sync กันเอง — แก้ที่เดียวอีกที่ไม่รู้เรื่อง และไม่มี error ขึ้น**<br>**นับยอดผลิต/เงิน/รายงาน → ใช้ `order_items` เสมอ** (ครัว·แมส·ออเดอร์·P&L อ่านที่นี่หมด)<br>`mp_deliveries` ใช้ดู**รอบส่ง/สถานะ MP** ได้ · **ห้ามใช้ `box_count` เป็นตัวนับกล่อง** | หลักฐาน 14 ส.ค. — Milk `MP-0814-027`: `mp_deliveries` 7 กล่อง vs `order_items` 6 แถว → **หน้าครัวโชว์ 6 · หน้าแพลนโชว์ 7** นัทจับได้เองจากการเทียบ 2 จอ<br>✅ **U แก้แล้ว 14 ส.ค.** — ย้าย sync เป็น cron `/api/mp-sync` ทุก 10 นาที (เดิมอยู่ในเบราว์เซอร์ ใครเขียน DB ตรงๆ ไม่ทำงาน) · ตรวจสดได้ที่ `/api/mp-sync?dry=1`<br>⚠️ **แก้ข่าว: ประโยคเดิมที่เคยเขียนว่า "`mp_deliveries.menu_items` คือของจริงที่ KQ ใช้" กลับด้าน — ของจริงคือ KQ อ่าน `order_items` เป็นหลัก · `mp_deliveries` เป็นแค่ fallback ตอน `order_items` ว่าง** (ฟ้าไล่โค้ด `kitchen_queue.html:451-545` ยืนยัน 15 ส.ค.) |
| 10 | **ผูก `mp_deliveries ↔ orders` ด้วยชื่อ+วันที่ไม่พอ ต้องมี `mp_type` ด้วย** | ลูกค้าคนเดียวสั่งหลายแบบในวันเดียวได้ |
| 11 | **จับคู่ข้อมูลห้ามใช้ "ชื่อ"** — ใช้ `order_id` → `line_uid` → `customer_id` เท่านั้น | ชื่อซ้ำ/สะกดต่าง = จับคู่ผิดคน |
| 12 | **`stock_total` (ลูกค้าเห็น) กับ `actual_stock` (ครัวนับ) ไม่การันตีว่าตรงกัน** | ลบกันตรงๆ ได้ยอดขายติดลบ |
| 13 | **ยอดขายมี outlier** — เมนูเดียวสัปดาห์เดียว 28 ชิ้น ปกติ 2-8 | ใช้ค่าเฉลี่ยจะแนะผลิตเกิน **ต้องใช้ median** |
| 14 | **ใบยอด ฿0 มี 3 ความหมาย** (รอบส่งเพิ่ม · อาหารพนักงาน · prepaid) — ห้ามเหมารวม | นับ KPI ผิดทั้งกระดาน |
| 15 | **`HS-` = log ระบบเก็บแต้ม ไม่ใช่ยอดขาย** | เคยสรุป "ยอดตก 37-49%" จากฐานผิด แล้วต้องถอนทั้งหมด |
| 16 | **Hato เก็บ "เมนูจริงที่ลูกค้าเลือกในเซ็ต" ไว้ใน *ชุดตัวเลือก (modifier)*** — ตอน sync ไม่ได้เก็บมาด้วย | ครัวเห็นแค่ชื่อเซ็ต ไม่เห็นว่าข้างในมีอะไร ⚠️ **ยังไม่ยืนยันว่าแก้แล้วหรือยัง** |

---

## 👥 คน กระบวนการ และการรายงาน

| # | บทเรียน | ที่มา |
|---|---|---|
| 17 | **ข้อมูลเทสที่ใช้ชื่อลูกค้าจริง = ระเบิดเวลา** | `U-0806-001` ดูเหมือนออเดอร์จริง เกือบทำครัวทำอาหารผิด |
| 18 | **สร้างเครื่องมือ ≠ มีข้อมูล** | สร้างช่องกรอก subcode แล้วประกาศว่า "ใช้ได้แล้ว" ทั้งที่ยังว่างทุกแถว |
| 19 | **ไฟล์ที่ไม่ commit = ของที่จะหาย** | agent ของเตียงหายตอนย้ายเครื่อง |
| 20 | **ป้ายประเภทงานผิด = อันตรายตอน automate** | ใบร้อยเอ็ดติดป้าย `messenger` ทั้งที่เป็นขนส่งตจว. — ไม่เป็นไรตอนคนทำ แต่พังตอนบอททำ |
| 21 | **"ชื่อกำกวม" อาจแปลว่าเราค้นหลวมไป ไม่ใช่ข้อมูลไม่พอ** | เคส Milk: 3 ห้องสรุปว่าหาไม่เจอ ที่จริงค้นไม่ละเอียดพอ |
| 22 | **เครื่องมือที่เคยถูก ไม่ได้แปลว่ายังถูก เมื่อบทบาทของต้นทางเปลี่ยน** | สคริปต์ sync ที่เคยแม่น กลายเป็นผิดหลังปิด Hato |
| 23 | **ข้อมูลเก่าคือศัตรู** | CC เคยรายงานงานที่ทำเสร็จแล้วว่า "ยังค้าง" 3 รายการ |
| 24 | **ถามแบบไม่ใส่สมมติฐาน** — ถาม *"X คืออะไร"* ไม่ใช่ *"X ยังทำงานอยู่ไหม"* | เข้าใจ `HS-` ผิดทั้งคืน เพราะตั้งคำถามที่ฝังสมมติฐานไว้แล้ว |
| 25 | **ถามเจ้าของก่อนขุด forensics** | ข้อมูลบอกได้แค่ "มีอะไร" บอกไม่ได้ว่า "มันคืออะไร" |
| 26 | **"จะทำคืนนี้" ≠ "ทำแล้ว"** | เคยประกาศทั้ง 16 ห้องว่า launch แล้ว ทั้งที่ยังไม่ได้ทำ |

---

## 🌳 git และการ deploy

| # | บทเรียน |
|---|---|
| 27 | **ห้าม `cp` ไฟล์จาก working tree ทับตอน deploy** — ย้อนงานคนอื่นหายเงียบ · แก้ใน worktree เท่านั้น |
| 28 | **ห้าม `git checkout <branch>` ในทรีหลัก** — หลาย session ใช้ทรีร่วมกัน · งานที่ยังไม่ commit ถูกล้างหายจริงมาแล้ว 2 รอบ |
| 29 | **push ต้องผ่าน worktree จาก `origin/main`** — ทรีหลักมีไฟล์ค้างของห้องอื่น ทำให้ rebase ล้ม · **ห้ามลบ/ย้ายของห้องอื่นเพื่อให้ push ผ่าน** |
| 30 | **เช็ค `origin/main` ก่อนสรุปว่ากู้ไม่ได้** — ชีทที่ประกาศว่าหาย อยู่ครบใน git |

---

## 📣 การตลาดและ SEO

| # | บทเรียน | ตัวเลขจริง |
|---|---|---|
| 31 | 🔑 **บทความเดียวแบก SEO ทั้งร้าน** — "ข้าวไรซ์เบอร์รี่ ข้อดี/ข้อเสีย" = **692 views = ~70% ของบล็อกทั้งเว็บ** | redirect ตัวนี้ห้ามพลาดเด็ดขาด |
| 32 | **AI/GEO อ้างอิงเราจริง แต่ยังไม่ส่งคนมา** — AI query **427 ครั้ง/30 วัน** (ChatGPT/Gemini/Claude/Perplexity) แต่ **sessions จาก AI = 0** | → คงโครง FAQ + heading คำถามในทุกบทความ |
| 33 | **แพทเทิร์น "ประโยชน์และโทษของ [X]" คนไทยเสิร์ชเยอะ** | คำที่ติดอันดับ 1-2: กลูเตนฟรี · ไรซ์เบอร์รี่ · น้ำปลา low sodium |
| 34 | **bounce 90% · 1.1 หน้า/ครั้ง** — คนมาจาก Google อ่านบทความเดียวแล้วออก | → ต้องมี internal link / related posts / CTA ท้ายบทความ |
| 35 | **0 คนใช้โค้ดที่ต้องทักแอดมิน · โค้ดที่กรอกเองได้มีคนใช้จริง** — `FREEWEEKDAY` ลดรวมแค่ ฿531 แต่ทำยอด ฿10,033 | **friction ฆ่า offer** → gift item system = งานเบอร์ 1 |
| 36 | **Wix ยอดขาย ฿0** — ทิ้งได้ไม่เสียยอด **เสียแค่ SEO 10 ปี** | ตัดสินใจ cutover ได้สบายใจ |

---

## 🍳 ครัวและปฏิบัติการ

| # | บทเรียน |
|---|---|
| 37 | **Meal Plan ส่งได้แค่ จ/พ/ศ บ่าย = ข้อจำกัดการผลิตจริง ห้ามผ่อนกฎ** — ครัวเข้า 8:00 แต่ผักมาเกือบ 9:00 |
| 38 | **root cause ที่แก้ได้จริง: ให้ผักมาก่อน 06:00 ของวันก่อนหน้า** → ต้องแพลนผลิตล่วงหน้าได้ = **เหตุผลเชิงธุรกิจของน้องนิว+ฟ้าที่ชัดที่สุด** |
| 39 | **ครัวอ่านไทยได้ 2/6 คน** — เครื่องมือทุกตัวต้องมีตัวเลข+รูป+ไอคอน ห้ามพึ่งตัวหนังสือล้วน |
| 40 | **แปลอักษรไทใหญ่ = ไร้ประโยชน์** (เขาอ่านไม่ได้) → ใช้ **ไทย + พม่า** คู่กัน |
| 41 | **Hato ไม่มีระบบแยกวันส่ง — เดินด้วย man power ล้วน** → ยอดที่ Hato รายงานเว่อร์เกินจริง เพราะนับใบที่แอดมินแยกเป็น "ออเดอร์" |

---

## 📍 ที่มาของแต่ละข้อ (ถ้าอยากอ่านฉบับเต็ม)

| ข้อ | ไฟล์ต้นทาง |
|---|---|
| 1-8, 17-30 | `docs/archive/handoff/HANDOFF_CC_20260805.md` · `HANDOFF_P_20260808.md` · `CLAUDE.md` |
| 9 | `docs/archive/kitchen_daily/FAH_20260810_today.md` |
| 22 (สาเหตุคอร์สหาย ฉบับเต็ม) | `docs/archive/kitchen_daily/FAH_20260811_final.md` |
| 31-36 | `docs/archive/migration/WIX_SEO_HARVEST.md` · `web/eath/*` |
| 37-41 | `docs/KITCHEN.md` · `CLAUDE.md` |

⚠️ **`CLAUDE.md` ยังมีลิงก์ชี้ไป `docs/HANDOFF_CC_20260805.md`** ซึ่งย้ายเข้า `docs/archive/handoff/` แล้ว — **บทเรียนในนั้นถูกยกมาไว้ที่ไฟล์นี้ครบแล้ว ไม่ต้องตามไปอ่าน**

---

# 🔎 ภาคผนวก — ของที่เจอตอนอ่านลึกทุกไฟล์ (15 ส.ค. 2026)

> รอบแรก pm อ่านแค่หัวไฟล์ 3 บรรทัด → นัทสั่งให้อ่าน 12-20 บรรทัดทุกไฟล์
> **ผลคือเจอของที่ไม่เคยมีใครหยิบไปทำต่อ 11 เรื่อง** ข้างล่างนี้

## 🚨 ของที่อาจหายถาวรแล้ว — ต้องเช็คด่วน
| เรื่อง | รายละเอียด | อยู่ใน |
|---|---|---|
| **เมนูจริงในเซ็ตที่ sync มาไม่ครบ** | Hato เก็บเมนูที่ลูกค้าเลือกไว้ใน "ชุดตัวเลือก (modifier)" — **พบ 36 ใบ (~17% ของออเดอร์)** ที่ครัวเห็นแค่ชื่อเซ็ต ไม่เห็นข้างใน · **Hato ปิด 7 ส.ค. → ถ้าไม่ได้ดึงก่อนวันนั้น = หายถาวร** | `docs/ASK_ADMIN_MODIFIER.md` |

## 💡 สัญญาณจากลูกค้าจริงที่ไม่เคยเข้าโรดแมพ
| เรื่อง | หลักฐาน |
|---|---|
| 🥬 **ลูกค้าขอเมนูมังสวิรัติ — เราไม่มีเลย** | อ่านแชท LINE OA จริง · แอดมินตอบไม่ได้ |
| 🚚 **ตจว. มี demand จริง** ลูกค้าถามหาเอง | แชท LINE OA |
| ❌ **ของหมด = friction** ลูกค้าถามสต็อกก่อนสั่ง | แชท LINE OA |
| ✅ **คูปองได้ผลจริง** (โค้ด JULY100) | แชท LINE OA |

## 🎁 ของมีค่าที่มีอยู่แล้วแต่ไม่ได้ใช้
| ของ | ตัวเลขจริง |
|---|---|
| **วันเกิดลูกค้าในระบบ** | **2,165 คน (27% ของฐาน)** · เดือน 7 อย่างเดียว = 186 คน **ยิง LINE ได้ 100%** · ในนั้นเคยซื้อจริง 125 คน รวม **฿1,026,955** |
| **อินฟลูที่ฟอลโล่เรากลับ** | `@nattietrainhard` (IG 169K) = **คนเดียวที่ mutual** จาก 21 คนที่เช็ค = **priority #1 ทาบ** |
| **หมวดสินค้าที่ไม่เคยทำคอนเทนต์เลย** | **BABY FOOD อาหารเด็ก 29 เมนู** · แซลมอน/กะพงสด · โบนบรอธ · บ๊ะจ่าง |
| **รูป Meal Plan** | ⚠️ **มีแล้ว 6 รูป** กู้จาก Hato CDN ก่อนดับ (verify HTTP 200 · อยู่ใน bucket เราเอง) — **แต่ `CLAUDE.md` ยังเขียนว่า "ขาดรูป Meal Plan" อยู่** |

## 🏹 คู่แข่งขยับที่ต้องรู้
**Passion Food (ภัยหลักอันดับ 1) pivot ไปขายคอร์สสอนธุรกิจ** — FOOD DELIVERY 101 · ฿7,900 · 30 ที่นั่ง = **฿237,000 ต่อรอบ 1 วัน** (เทียบยอดขายอาหารทั้งเดือน)
→ สูตรที่เขาใช้ถอดไว้แล้วใน `web/eath/competitor_passionfood_course_pivot.md`

## 🕳️ ระบบที่ประกาศว่ามี แต่จริงๆ ไม่เคยถูกสร้าง
| ของ | ความจริง |
|---|---|
| ตาราง `weekly_broadcast` · `customer_requests` · `menu_history` | **404 ไม่เคยถูกสร้าง** |
| `mp_deliveries.customer_request` | **ว่างทุกแถว** (67 แถว มีฟิลด์ ไม่มีข้อมูล) |
| `request_opens_at` / `request_deadline` | **null ทั้งหมด** — ระบบเปิดรอบรีเควสไม่เคยทำงานจริง |
| **Hato ไม่มี field "จำนวนผลิต"** | ยืนยันผ่าน GraphQL จริง → นิวต้องรอแอดมินกรอกในระบบเรา |

## 📉 รายงานที่ห้ามเอาไปสรุป
**`download/hato_final/lineitem_summary.md`** — ยอดรายเดือนปี 2026 **เกือบว่าง** (ม.ค. บอก ฿13,522 แต่ของจริง ฿581,910)
→ ใช้ได้เฉพาะช่วง 2024–2025 · **ปี 2026 ให้ใช้ `scripts/finance/orders.mjs` เท่านั้น**

## 🎓 บทเรียนเพิ่ม
**42. ห้ามเดา "สัปดาห์ของเมนู" จากยอดขาย** — ของค้างขายยาวข้ามหลายสัปดาห์ พอเจอยอดเก่าก็ลากย้อนผิด (`D105` กลายเป็น "4-10 May" ทั้งที่เป็นของ 27 ก.ค.) → **ตารางหมุนเมนูเป็นสิ่งที่เจ้าของกำหนด ยอดขายบอกไม่ได้**
