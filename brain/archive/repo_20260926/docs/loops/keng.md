# ของค้างห้อง keng (ไฟล์นี้ห้องนี้เขียนคนเดียว — ห้องอื่นห้ามแตะ)

## 🛵 ห้องพี่เก่ง (แพลนส่ง/Lalamove) — เรื่องค้างกับนัท (เพิ่ม 20 ส.ค. ตามกฎ `RULE_FOLLOW_UP.md`)

> ป้าย: 🔴 รอนัทเคาะ · 🟡 เก่งทำต่อได้เอง · 🔵 รอคนอื่น · 💤 พัก/ไอเดีย · ⚰️ ตาย
> ⚠️ ตามกฎ "นัทเงียบ ≠ เรื่องจบ" — ปิดได้ทางเดียวคือนัทเคาะ/ปัดตก **หรือพิสูจน์ได้ว่าเรื่องนั้นหมดความหมายไปแล้ว**
> 🔄 **ติดป้าย 10 ก.ย. 2026 — ตรวจกับโค้ด/เอกสารจริงทุกข้อ ไม่ได้ติดตามที่เขียนไว้เดิม → เจอ 2 ข้อที่ตายไปแล้วแต่ยังค้างอยู่ในลิสต์**

| เรื่อง | ค้างตั้งแต่ | ค้างตรงไหน | รอใครเคาะ |
|---|---|---|---|
| 🔴 **รัน `scripts/sql_delivery_trust.sql`** | 16 ส.ค. | **เงื่อนไขก่อนเปิดจองรถจริง** · ตอนนี้ระบบเดาหมุดให้ลูกค้าที่ไม่ปักหมุด แต่ในฐานข้อมูล **หมุดที่เดากับหมุดจริงหน้าตาเหมือนกันทุกอย่าง** → ด่านกันของไปผิดที่ยังทำงานไม่เต็ม · ไฟล์เดียวจบ เพิ่มคอลัมน์อย่างเดียว ไม่ลบไม่แก้ข้อมูลเดิม | **นัท** กดรันใน Supabase |
| 🔴 **เปิดสวิตช์ `LALAMOVE_BOOKING_ENABLED=1` เมื่อไหร่** | 16 ส.ค. | ระบบจองรถทำเสร็จ+เทสผ่านแล้ว แต่ล็อกไว้ตามที่นัทสั่ง (*"ยังไม่ทำจริงจนกว่าจะเทสสำเร็จ"*) · เปิด = ตั้ง env ตัวเดียวใน Vercel ไม่ต้องแก้โค้ด · **ควรรอ SQL ข้างบนก่อน** | **นัท** |
| ⚰️~~รอบที่ "ข้ามช่วงเวลา" ยอมได้ไหม~~ **ปิดแล้ว 26 ส.ค.** | 6 ส.ค. | นัทตอบ: เฉพาะคนเลือก "รับได้ทั้งวัน" จับข้ามได้ · **ระบบทำอยู่แล้ว ไม่ต้องแก้** · ⚠️ ตัวเลข ฿144-177 ที่เคยแจ้ง **ผิด ถอนแล้ว** — วัดใหม่ได้ ฿0-7/วัน (คนเลือกทั้งวันมีแค่ 0-1 คน/วัน) ดู `docs/KENG_SUMMARY.md` | — |
| ⚰️ ~~**เลิกใช้เพดานระยะจับคู่**~~ **ทำไปแล้ว 21 ส.ค. — ข้อนี้ตาย** | 14 ส.ค. | นัทเคาะเองแล้ว (*"เอาเลย"*) → เอาเพดานออก ให้ราคาตัดสินแทน · **verify 10 ก.ย.: `MSG_MAX_PAIR_KM` ไม่มีในโค้ดแล้ว** (เหลือแค่ `MSG_FALLBACK_PAIR_KM` ใช้เฉพาะวันงานเกิน 16 จุด) · ผลวันนั้น **12 รอบ → 6 รอบ · ประหยัด 32%** · ⚠️ ข้อนี้ค้างในลิสต์ 20 วันทั้งที่จบไปแล้ว | — |
| 🔴**มาตรฐานเดียวเรื่องคอร์สหลายรอบ** | 6 ส.ค. | แอดมินทำ 2 แบบปนกัน — บางเคสเปิด "ใบแยกวันส่ง" (ระบบเห็น) บางเคสจดในชีทเฉยๆ (ระบบไม่เห็น) · **นี่คือต้นเหตุที่ 3 ทางไม่เคยตรงกัน** | **นัท** เคาะมาตรฐานเดียว |
| 🔴**ชีทใบแมสของแอดมิน — จะเอายังไงต่อ** | 7 ส.ค. | แท็บรายวัน **ไม่ได้ถูกสร้างทุกวัน** (ลองดึง 4 รูปแบบชื่อ ไม่เจอ) → **"ตรวจ 3 ทาง" ทำเป็น routine ไม่ได้เลย** เหลือเทียบได้แค่ 2 ทางที่อ่านตารางเดียวกันอยู่แล้ว = แทบไม่มีประโยชน์ | **นัท** — เลิกใช้ชีทมือ หรือสั่งให้แอดมินสร้างทุกวัน |
| ⚰️ ~~**ต้นทุนแมสร้านต่อเที่ยว**~~ **ตาย — ไม่มีแมสให้เทียบแล้ว** | 5 ส.ค. | เดิมขอไว้เพื่อเทียบว่า "จ้างแมสคุ้มกว่ายิง Lalamove เมื่อไหร่" · **แต่ 22 ส.ค. นัทบอกเอง _"แมสตอนนี้ลาออกไปหมด ใช้ลาล่ามูฟ"_** (`docs/OPERATIONS_REALITY.md`) → **ไม่มีทางเลือกให้เทียบอีกแล้ว คำถามหมดความหมาย** · ตัวเลขฝั่ง Lalamove ที่เก็บไว้ (฿817/วัน ~฿21,200/เดือน) ย้ายไป `docs/UNDER360_HISTORY.md` หัวข้อ keng แล้ว | — |

---

