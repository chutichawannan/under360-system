/* ════════════════════════════════════════════════════════════════════
   ตัวช่วยให้โถงหลังบ้านติดตั้งเป็นแอปลงเครื่องได้ (นัทขอเอง 22 ก.ย. 2569)

   🔴 ขอบเขตอยู่แค่ /staff/ เท่านั้น — ห้ามขยาย
      เหตุผล: ไฟล์ชุดนี้เสิร์ฟบนโดเมนเดียวกับแอปสั่งอาหารของลูกค้า
      ถ้าขอบเขตกว้างกว่านี้ ตัวนี้จะไปคุมหน้าลูกค้าด้วย = เสี่ยงลูกค้าเห็นของค้าง

   🔴 ไม่เก็บ cache อะไรทั้งสิ้น — ต่อเน็ตไม่ได้ก็ให้มันฟ้องไปตรงๆ
      ดีกว่าโชว์ยอดขาย/ออเดอร์เก่าค้างแล้วคนอ่านเข้าใจผิด
      (มี fetch handler เพราะ Chrome บังคับถึงจะขึ้นปุ่มติดตั้ง)
   ════════════════════════════════════════════════════════════════════ */
self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', e => e.waitUntil(self.clients.claim()));
self.addEventListener('fetch', e => { e.respondWith(fetch(e.request)); });
