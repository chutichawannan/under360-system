# ของค้างห้อง u (ไฟล์นี้ห้องนี้เขียนคนเดียว — ห้องอื่นห้ามแตะ)

## 🧑‍💻 ห้อง U-maintainer (ระบบ/โค้ด) — ยุบที่จดเข้ามา 10 ก.ย. 2569 · **ติดป้ายครบทุกข้อ (เฟส 2)**
> ย้ายมาจาก `docs/OPEN_LOOPS_U.md` + `docs/U_ROOM_STATUS.md` + กวาด `docs/HANDOFF_U_FINAL_20260825.md` ซ้ำอีกรอบ (ทั้งหมดอยู่ครบ ไม่ได้ลบ)
> **รวม 25 ข้อ** · 🔴 6 · 🟡 9 · 🔵 2 · 💤 2 · ⚰️ 6
> ข้อที่เขียนว่า "verify 10 ก.ย." = เปิดโค้ด/query DB จริงวันนี้ · ข้อที่ไม่ได้เขียน = ยกมาตามเดิม ยังไม่ได้ตรวจซ้ำ (บอกไว้ตรง ๆ กันเข้าใจผิดว่าทุกข้อสด)

### 🔴 รอนัทเคาะ (6)
| # | เรื่อง | ค้างตั้งแต่ | ค้างตรงไหน |
|---|---|---|---|
| 🔴1 | **การ์ด "เมนูพิเศษ ประจำสัปดาห์" หน้าแรก — เอาคืนไหม** | 20 ส.ค. | verify 10 ก.ย.: ยัง**ซ่อนอยู่จริง** (`home_layout` การ์ด `card_1782208127869`) · เป็นทางลัดไปเมนูสัปดาห์ ไม่ใช่การ์ดโปรฯ ที่พลอยสั่งตัด · เปิดคืนใช้ 10 วิ |
| 🔴2 | **ลูกค้าที่ประวัติ/แต้มหลุด** | 19 ส.ค. | ⚠️ ตัวเลขเดิม "32 คน ฿662,115" **ใช้ไม่ได้** — นับสดใหม่ไม่ตรง และสาเหตุคนละเรื่อง (`total_spent` ไม่เคยถูก maintain) · ฝั่ง "มองเห็นประวัติ" แก้แล้ว · **แต้ม/tier ยังไม่มีระบบสะสมตั้งแต่แรก** (ผูกกับข้อ ⑱ ในไฟล์นี้) |
| 🔴3 | **มุมมองชิป PTT "แทบไม่ต่าง"** (ข้อ 5 รอบเทส 17 ส.ค.) | 17 ส.ค. | U ถามกลับว่าอยากให้ต่างยังไง ยังไม่ได้คำตอบ |
| 🔴4 | **สต็อคของครัวหายตอนไหน** | 20 ส.ค. | ตอนนี้ลดตอนกด "จัดกล่องเสร็จ" · อยากให้ไปลดตอน "ส่งแมสแล้ว" แทน = แก้บรรทัดเดียว · ⚠️ **ทับกับ ISSUE 02 แล้ว** — ถ้าประชุมนั้นเคาะ ข้อนี้ปิดตาม |
| 🔴5 | **ส่วนลด VIP ตอนแอดมินสั่งแทนในหน้า OH** | 22 ส.ค. | หน้านั้นคิดเงินคนละเส้นทาง ยังไม่คิดส่วนลดให้ · ถามไปแล้วรอเคาะ |
| 🔴6 | **วันหมดอายุของแถม `JAYOLD` (ซาลาเปาเจ)** | 10 ก.ย. | verify 10 ก.ย.: `promo_codes.expires_at = null` **และ** `kitchen_data.order_gifts.jayold` ไม่มี `expires_at` → **ของแถมไม่มีวันจบ** · เทศกาลเจจบ 18 ต.ค. · เป็นเรื่องแจกของ = นัทเคาะ · **นัทบอกวันเมื่อไหร่ ผมตั้งให้ทั้ง 2 ที่พร้อมกัน** (ตั้งที่เดียว = ลูกค้ากรอกโค้ดผ่านแต่ไม่ได้ของ) |

### 🟡 ห้อง u ทำต่อได้ ไม่ติดใคร (9)
| # | เรื่อง | สถานะ |
|---|---|---|
| 🟡1 | **รับ `fbclid` + ตัวสร้างลิงก์ติดป้ายแอด** | verify 10 ก.ย.: `grep fbclid` บน `origin/main` = **0 ครั้ง ยังไม่เคยขึ้นจริง** · โค้ดเสร็จ เทส 18/18 อยู่บน branch `feat/fbclid-capture` · ค้างตั้งแต่ 20 ส.ค. · ต้องประสานชื่อพารามิเตอร์กับ **ห้อง M** ก่อน |
| 🟡2 | **`api/health-critical.js` ชี้ประตู LIFF เก่า** | verify 10 ก.ย.: `LIFF_OK='2010442513-NI3JGTkb'` (`:31`) · **ตอนนี้ยังถูก** เพราะประตูเก่ายังเปิด · วันปิดประตูเก่าเมื่อไหร่ ตัวเช็คจะเตือนผิดทันที |
| 🟡3 | **ย้ายวันส่งแล้วต้องโชว์วันใหม่ทุกระบบ** (ครัว/แพลนเมนู/แมส/ใบจัดของ) + วันเดิมเหลือแถวจางบอกว่าย้ายไปไหน | ยกมาจาก HANDOFF 4.1 · **ยังไม่ verify รอบนี้** |
| 🟡4 | **N-65** แพลนเมนูโชว์เฉพาะวันที่เลือก · **N-66** ลบ/ย้ายใบที่เลื่อนส่ง · **N-67** ช่องค้นหาเมนู · **N-68** สั่ง Hyrox แทนลูกค้าแล้วชื่อไม่ขึ้น | ยกมาจาก HANDOFF 4.1 · **ยังไม่ verify รอบนี้** |
| 🟡5 | **`order_items` แทรกซ้ำ → ครัวเห็นกล่องเกิน** (มิ้ม 15/7 · วิไลลักษณ์ 13 · Milk ×2) + `order_items` ไม่ sync กับ `mp_deliveries` (จุฑามณี) | ยกมาจาก HANDOFF 4.1 · **ยังไม่ verify รอบนี้** · ⚠️ เกี่ยวกับสต็อกโดยตรง ควรตรวจคู่กับ ISSUE 02 |
| 🟡6 | **ใบสั่งผลิตหน้าครัว** — รวมยอดเมนู×กล่องให้ถูก + โชว์ใบที่ยังไม่ได้แพลน + เน้นเมนูพิเศษสัปดาห์ | ยกมาจาก HANDOFF 4.1 · ⚠️ **ทับกับ ISSUE 01/02 หน้า A-B ที่กำลังออกแบบอยู่** |
| 🟡7 | **N-37/38 ลูกค้าเลือกเมนูเข้ารอบเอง · N-41 ถามข้อจำกัดอาหารตอนสั่ง MP** | ยกมาจาก HANDOFF 4.1 · **ยังไม่ verify รอบนี้** |
| 🟡8 | **กิ๊ฟสร้างโค้ดส่วนลดไม่ได้** | verify 10 ก.ย.: **น่าจะจบไปแล้ว** — มีโค้ดถูกสร้างหลัง 25 ส.ค. **11 ตัว** (MP10 · MP15 · WEEK120 · NEW200 · DISCOUNT10 · MP99 · MP119 · JAYOLD ฯลฯ) และ MP99/MP119 **ถูกใช้จริง 5 ครั้ง = ส่วนลดหักได้จริง** · เหลือแค่ให้กิ๊ฟยืนยันด้วยปากตัวเองว่าสร้างเองได้แล้ว |
| 🟡9 | **แถวจางบอก "ใบนี้อาจซ้ำ"** — ลูกค้า 3 คนมีใบ HP+HP วันเดียวกัน (แชมป์ · ฝน · พจนา) | ยกมาจาก HANDOFF 4.4 · เดิมเขียนว่า "ต้องให้กิ๊ฟเช็คสลิป" → **ฝั่งระบบทำได้เอง**: มี `dupMate()` ติดป้ายใบซ้ำอยู่แล้ว แค่ยังไม่ครอบเคส MP |

### 🔵 รอห้องอื่น (2)
| # | เรื่อง | รออะไร/ใคร |
|---|---|---|
| 🔵1 | **N-76/N-77 การ์ดสีฟ้า + จัดของล่วงหน้า** | **ห้องฟ้า** จองไว้ — u ไม่แตะ กันแก้ไฟล์ชนกัน |
| 🔵2 | **ยุบช่องแพ้อาหาร 2 ช่องเป็นช่องเดียวไหม** | **พลอย** (ไม่ใช่นัท) · u เก็บ 2 ช่องไว้โดยตั้งใจ — ช่องบนครัวเห็นเป็นแถวแดง "ตัดออกเด็ดขาด" ถ้ารวมกับ "ขอข้าวนิ่ม/ไม่เผ็ด" ครัวแยกไม่ออกว่าอันไหนห้ามจริง (เคสลูกค้าโรคไต 13 ส.ค.) = เรื่องความปลอดภัยอาหาร |

### 💤 พัก / ไอเดีย (2)
| # | เรื่อง | ทำไมพัก |
|---|---|---|
| 💤1 | **APK ปริ้นใบอัตโนมัติเบื้องหลัง** (นัทถาม 25 ส.ค. *"ทำยังไงให้ run background"*) | ตอบไปแล้วว่าทำได้ ไม่ต้องผ่าน Play Store · **สเปคตกลงไว้ครบแล้ว อย่าออกแบบใหม่**: ต้องเป็นบริการเบื้องหน้า · แอปเช็คออเดอร์เอง · วาดใบด้วยเครื่องมือ Android (ไทยออกครบ) · build ผ่าน GitHub Actions · ⚠️ `gh` ยังไม่ล็อกอินบนเครื่องนี้ · **ยังไม่เริ่มเขียน ไม่มีใครทวง** · ระหว่างนี้ใช้ 3 ตั้งค่าบนเครื่อง POS ที่บอกนัทไปแล้ว |
| 💤2 | **เมนูสัปดาห์ตั้ง `available_from` = ดันวันส่งทั้งตะกร้าไปวันช้าสุด** | กำแพงห้ามปนตะกร้าลบแล้ว (`e62f08c`) เหลือแค่ "ลูกค้าอาจไม่คาดคิด" · มีป้ายบอก 2 จุด · **ยังไม่รู้ว่าลูกค้าจริงอ่านทันไหม** → 2 ทางที่นัทเคาะได้: (ก) ปล่อยแบบนี้ (ข) แยกกล่อง "ส่งได้เลย / ส่งวันที่ X" · **ไม่มีใครเดือดร้อนตอนนี้ = พักไว้** |

### ⚰️ ตาย — จบไปเองแล้ว (6) *(ห้ามลบแถว ตามกฎ)*
| # | ~~เรื่อง~~ | ตายเพราะ (verify 10 ก.ย.) |
|---|---|---|
| ⚰️1 | ~~D4 (`D131`) เปิดหรือปิดขายกันแน่~~ | **ปิดสนิทแล้ว** `is_available=false` · `stock_total=0` (เดิมค้างเพราะยังมีของเหลือ 6) |
| ⚰️2 | ~~`2A` ข้าวตุ๋นแซลมอน (BABY FOOD) เปิดคืนไหม~~ | **เปิดขายอยู่แล้ว** `is_available=true` เหลือ 21 |
| ⚰️3 | ~~ยกเลิกออเดอร์แล้วสต็อกไม่คืน~~ | **มีตัวคืนแล้ว** `operation_hub.html:1724` → `odRestock(id,+1)` เรียก `adjust_stock` ตัวเดียวกับฝั่งลูกค้า · ⚠️ ที่ยังขาดคือ **หลักฐานว่าคืนสำเร็จ** (ไม่มี log) — ยกไปอยู่ใน ISSUE 02 แล้ว ไม่ต้องถือซ้ำที่นี่ |
| ⚰️4 | ~~`order_items` 3 แถวที่ `menu_item_id = null` → ไม่เคยตัดสต็อก~~ | **บริบทเปลี่ยน** — นับสด 10 ก.ย. ในใบ `U-` ตั้งแต่ 1 ส.ค. มี null 1,554 แถว **แต่ 1,552 เป็นเมนู Meal Plan/Hyrox ซึ่งไม่มีสต็อกอยู่แล้ว** · เหลือเมนูที่เกี่ยวกับสต็อกจริง **2 แถว** (`U-0824-014` S099 เซ็ตสารทจีน · `U-1008-001` G01 ของแถม) = ไม่ใช่เมนูที่ขายเกินได้ |
| ⚰️5 | ~~หน้า Meal Plan ใหม่ + ต่อเข้าตะกร้า (HANDOFF 2.1/2.2)~~ | นัทเคาะ 20 ส.ค. ให้รื้อป้อบเดิมแทน · **ทำเสร็จขึ้น main แล้ว** (ปฏิทินเมนู · เลือกเอง/จัดให้ · ราคาต่อมื้อ+ส่วนลด% · เลื่อนวัน · ข้ามรอบ · ปุ่มเปลี่ยนเนื้อเป็นหมู) |
| ⚰️6 | ~~ข้อจำกัดอาหารลูกค้า MP 7 คน~~ | ส่งต่อห้องครัว/ฟ้าแล้ว u ไม่ถือ |

### ⚠️ ข้อจำกัดของห้องนี้ที่นัทควรรู้ (ไม่ใช่ TODO — ห้ามนับรวมสถิติ)
1. **u เทสสั่งออเดอร์จริงไม่ได้** — กลัวขยะไปโผล่หน้าครัวแล้วครัวทำอาหารทิ้ง (เคยเกิดจริง 11 ใบ) → ดักการเขียนแล้วเทสแทน = พิสูจน์ได้ ~80% **ที่เหลือตกเป็นภาระนัท**
2. **Vercel ปิด Preview Deployment อยู่** → กฎ "ทำ branch แล้วส่งลิงก์ให้นัทเทส" ทำตามไม่ได้จริง

*(รายการที่ปิดไปแล้วฉบับเต็มพร้อมเหตุผลรายข้อ อยู่ใน `docs/archive/OPEN_LOOPS_U.md` — ย้าย ไม่ได้ลบ)*

---

