# 💰 สมุดรายรับรายจ่าย (ร้าน + ส่วนตัวนัท) — ใบงาน

> นัทขอเอง 12 ก.ย. 2026: *"ฉันอยากจดรายรับรายจ่าย ทั้งของฉันเองและของร้าน"*
> เลขาออกแบบให้ยึดหลักเดียว: **นัทต้องไม่ต้องเปลี่ยนพฤติกรรม** — ทุกวันนี้นัทพิมพ์บอกกะปันในไลน์อยู่แล้ว (เช่น "วันนี้กินแซมาอึลไป 956 บาท") ให้สิ่งนั้นกลายเป็นการจดบัญชีเลย

## หลักการ
1. **ป้อนด้วยการพิมพ์ในไลน์ตามปกติ** — ไม่มีฟอร์ม ไม่มีแอปใหม่ให้เปิด
2. **รายรับของร้านไม่ต้องจด** — ดึงจาก `orders` อัตโนมัติ (มีอยู่แล้ว ผ่าน `scripts/finance/orders.mjs`)
3. **สิ่งที่ต้องจดคือรายจ่าย + รายรับนอกระบบ** (เงินสด · โอนเข้าส่วนตัว · B2B วางบิล)
4. **แยก "ร้าน" กับ "ส่วนตัว" ตั้งแต่ตอนจด** ไม่ใช่มาแยกตอนสิ้นเดือน (กฎบัญชีกลางใน CLAUDE.md)
5. ⛔ ข้อมูลส่วนตัว **ห้ามออกมาเป็นไฟล์ใน repo** — อยู่ใน DB + หน้า PWA ที่ล็อกเท่านั้น

## โครงข้อมูล (ตารางใหม่ `ledger`)
```
at            timestamptz   เวลาที่เกิดรายการ
side          text          'shop' | 'personal'
kind          text          'out' (จ่าย) | 'in' (รับ)
amount        numeric
category      text          วัตถุดิบ · ค่าส่ง · แอด · เครื่องมือ · ค่าแรง · ของใช้ครัว · ส่วนตัว-อาหาร · ส่วนตัว-ลูก ฯลฯ
note          text          ข้อความดิบที่นัทพิมพ์ (เก็บไว้เสมอ ห้ามทิ้ง)
slip_url      text          รูปสลิปที่ส่งตามมา (ถ้ามี)
source        text          'line' | 'manual' | 'import'
created_by    text
```

## ทางเดินข้อมูล
```
นัทพิมพ์ในไลน์ → กะปันจับตัวเลข+คำ → เขียนลง ledger → ตอบสั้นๆ ว่าจดแล้ว (1 บรรทัด ไม่ถามซ้ำ)
รูปสลิปส่งตามมา → แนบกับรายการล่าสุดของคนนั้นอัตโนมัติ
หน้า PWA /pwa/money.html → ดู/แก้/ลบ + สรุปวันนี้·เดือนนี้ แยกร้าน/ส่วนตัว + export CSV ให้คนทำบัญชี
```

**กติกาการเดา:** ถ้าข้อความไม่ชัดว่าร้านหรือส่วนตัว → ลงเป็น `personal` ไว้ก่อน แล้วติดธง `ยังไม่ยืนยัน` ให้แก้ในหน้า PWA ได้ทีเดียวหลายรายการ
· คำที่ชี้ว่า **ร้าน**: วัตถุดิบ · ครัว · Freshket · GO · แก๊ส · ค่าส่ง · Lalamove · ยิงแอด · พนักงาน · ของใช้ร้าน
· **ห้ามกะปันถามย้อนทีละรายการ** (กฎเดิม: ห้ามรบกวนนัทรายข้อความ)

## แบ่งงาน
| ห้อง | ทำอะไร |
|---|---|
| **u-maintainer** | SQL ตาราง `ledger` + RLS · หน้า `pwa/money.html` (อ่าน/แก้/ลบ/สรุป/export · ล็อก PIN เหมือน PWA ตัวอื่น) |
| **kapan** | จับข้อความไลน์ของนัท → เขียน ledger + แนบสลิป · ตอบยืนยัน 1 บรรทัด |
| **f (การเงิน)** | เจ้าของหมวดหมู่ + เอา export ไปคุยกับคนทำบัญชี · ตรวจว่ารายการที่จดพอใช้ยื่นภาษีไหม |

## ที่ยังไม่ทำในเวอร์ชันแรก (กันบานปลาย)
- ไม่ทำ OCR สลิป · ไม่ต่อ API ธนาคาร · ไม่ทำงบกำไรขาดทุนอัตโนมัติ (statement ยังไม่มี)
- ไม่แตะเรื่องภาษี — **ตัวเลขทุกตัวต้องผ่านคนทำบัญชีก่อนใช้จริง**

---

# 🆕 v2 — นัทขยายโจทย์เอง 12 ก.ย. (ใช้ 2 คน: นัท + พลอย)

> คำนัท: *"สร้าง web app ที่บันทึกใช้ได้ทั้งฉันและพลอย มีหน้าเป็นของฉันเอง มีหน้าเป็นของพลอยเอง และมีหน้าที่เป็นของพวกเราทั้งคู่ · หน้าของฉันมีส่วนที่แยกทั้งส่วนตัวและร้าน Under360 · และทำให้ฉันดึงกะปันมาไว้ในห้องแชทนึง เพื่อพิมพ์แจ้งยอด"*

## โครงหน้า (4 หน้าในแอปเดียว)
| หน้า | ใครเห็น | เก็บอะไร |
|---|---|---|
| **ของนัท** | นัท | 2 แท็บย่อย: **ส่วนตัว** · **ร้าน Under360** |
| **ของพลอย** | พลอย | ของพลอยล้วน (นัทไม่ต้องเห็นรายละเอียดถ้าพลอยไม่อยากให้เห็น) |
| **ของเราสองคน** | ทั้งคู่ | ค่าใช้จ่ายบ้าน/ครอบครัว/ลูก · ใครออกเท่าไหร่ · ยอดคงเหลือระหว่างกัน |
| **สรุป** | ทั้งคู่ | ยอดเดือนนี้แยกกระเป๋า + export CSV |

## เพิ่มใน schema
```
owner   text   'nut' | 'ploy'          ← ใครเป็นคนจด
book    text   'personal' | 'shop' | 'shared'   ← กระเป๋าไหน (แทน side เดิม)
paid_by text   ใช้กับ book='shared' — ใครเป็นคนออกเงินจริง
```
**สิทธิ์:** แต่ละคนเห็น+แก้ของตัวเอง · หน้า shared เห็นทั้งคู่ · หน้าของอีกฝ่ายเปิดดูไม่ได้ (คนละ PIN / คนละ LINE uid)

## ช่องทางป้อน = ห้องแชทไลน์ที่มีกะปันอยู่
- **นัทสร้างกลุ่มไลน์ใหม่ แล้วเชิญกะปันเข้า** (บอทตัวเดิม ไม่ต้องทำบอทใหม่)
- พิมพ์ยอดในกลุ่มนั้น → กะปันจด · แยก owner จาก LINE uid ของคนพิมพ์ (นัท/พลอย)
- กลุ่มไหน = กระเป๋าไหน: แนะนำ **1 กลุ่ม = 1 กระเป๋า** (กลุ่ม "เงินเรา" = shared) เพราะไม่ต้องเดาและไม่ต้องพิมพ์คำกำกับ · ถ้าใช้กลุ่มเดียว ให้เดาจากคำแล้วติดธงให้แก้ทีหลัง
- ⛔ กะปันในกลุ่มนี้ **พูดแค่บรรทัดยืนยัน** ห้ามคุยเล่น (กฎเดิม)

## ดีไซน์
เลขาทำหน้าตัวอย่างให้นัทเปิดดูก่อน (มือถือเป็นหลัก) → นัทเคาะหน้าตา → **ห้อง u สร้างของจริง** ตามดีไซน์ที่เคาะแล้ว

---

# ✅ v3 — นัทเคาะดีไซน์แล้ว 12 ก.ย. → ห้อง u สร้างของจริงได้เลย

**คำนัท:** *"กำลังไปได้ดี แบบไหนก็ใช้ได้หมดนั่นแหละ เดี๋ยววันไหนใช้ไม่ได้เดี๋ยวก็ขอให้แก้เพิ่มเอง"*
→ **ไม่ต้องรอเคาะหน้าตาอีกรอบ** ทำตามดีไซน์นี้ไปก่อน แล้วนัทจะขอแก้เป็นรอบๆ เอง

**หน้าตัวอย่างที่นัทดูแล้ว (มือถือ):** https://claude.ai/code/artifact/db6eba6b-c503-4154-ac69-d19ac7077eef
ชื่อแอปที่ใช้ในดีไซน์ = **"สมุดเงินเรา"**

## สิ่งที่ดีไซน์ล็อกไว้ (ทำตามนี้)
- แถบบนสุด 4 แท็บ: **ของนัท · ของพลอย · เราสองคน · สรุป** — แท็บ "ของนัท" มีแถวปุ่มย่อย **ส่วนตัว / ร้าน Under360**
- หัวหน้า = ยอดจ่ายเดือนนี้ตัวใหญ่ + ยอดรับเข้าเล็กด้านขวา + แถบสัดส่วนหมวด 4 หมวดบนสุด
- รายการจัดกลุ่มตามวัน (วันนี้ / เมื่อวาน / วันที่) มียอดรวมท้ายหัววัน · แต่ละแถว = ไอคอน + ชื่อ + ป้ายหมวด + จำนวนเงิน
- รายการที่กะปันเดากระเป๋าไม่ชัด ติดป้าย **"ยังไม่ยืนยันกระเป๋า"** สีเหลือง แตะแล้วยืนยันได้ทันที
- หน้า "เราสองคน" ต้องบอกว่า **ใครออก** ในป้ายหมวด · หน้า "สรุป" มีบรรทัด **"ใครออกไปก่อนมากกว่า"** + รายรับร้านที่ดึงจากออเดอร์เอง (ไม่ต้องจด)
- ล็อกด้วย LINE uid ของแต่ละคน (ไม่ใช่ PIN ร่วม) — หน้าของอีกฝ่ายเปิดดูไม่ได้

## รายการจริงรายการแรกที่นัทพิมพ์เข้ามาแล้ว (เอาเข้า DB ตอน seed)
| วันที่ | กระเป๋า | รายการ | ใครออก | ยอด |
|---|---|---|---|---|
| 12 ก.ย. 2026 | shared | กินข้าวกับพลอย | นัท | 1,471 |
