# 📋 ทะเบียนเรื่องค้าง — Under360

> อัปเดต **13 ส.ค. 2026** โดย CC (เลขา) · **ตรวจกับ DB จริง + บอร์ด ไม่ใช่คำบอกเล่า**
> นัทบอกเอง: *"ฉันเอาแต่ส่งเรื่อง แต่ไม่ได้อ่านสิ่งที่นายตอบเลย"* → **ไฟล์นี้คือที่ที่ของไม่หาย**

---

## ① เรื่องที่นัทแจ้ง 12–13 ส.ค. — แก้เสร็จแล้ว ✅

| เรื่อง | ผล |
|---|---|
| ใบจัดของ: รหัส `3 6 8 10 11` ไม่มีชื่อเล่นและไม่มีโค้ด | ✅ พิมพ์ผิดใน regex (`/^Nod/` ตกเครื่องหมาย `\`) → `No3` เหลือ `"3"` · แก้แล้ว commit `6f199a4` |
| ใบจัดของ: ต้องมีวันที่สั่ง | ✅ เพิ่มแล้ว |
| ใบจัดของ: เอากล่องเตือนยอดไม่ลงตัว (N-61) ออก | ✅ เอาออกแล้ว |
| KQ ตั้งสต็อค: ขอแถบหมวดด้านบน | ✅ แถบหมวด + ช่องค้นหา · commit `5f156c6` |
| ปิดรับส่งวันอาทิตย์ 16 ส.ค. (ชดเชยวันแม่) | ✅ ล็อกวัน + ขึ้นเหตุผล · หมดวันหายเอง |
| "ของทีม" ที่ CC กันออกจากสถิติ 9 แถว | ✅ นัทยืนยันเป็นลูกค้าจริงหมด → นับกลับแล้ว |
| ตอบ 4 คำถามชีท Meal Plan + เมนูที่อรรถพล/พรรณพิไลยังไม่ได้ทาน | ✅ ตอบครบ (ความครบข้อมูล 97%) |

---

## ② เรื่องที่นัทแจ้งแล้ว — **ยังไม่แก้** 🔴

| เรื่อง | สถานะจริง | อยู่ที่ |
|---|---|---|
| **หน้า DB ↔ KQ ซิงค์สมบูรณ์ไหม** | ❌ **เชื่อมทางเดียว** — KQ เขียน `stock_total` ได้ แต่หน้า DB อ่านสำเนาตัวเอง ไม่เคยอ่านกลับ → ครัวกรอกคืนนี้ เช้ามาแอดมินแก้ = **ทับหายเงียบ** | U |
| **เมนูออนไลน์ใน DB ต้องตรงกับ KQ** | ❌ **22 เมนูเปิดขายแต่ไม่มีแถวในหน้า DB เลย** (รวมชุด HYROX 8 ตัว) · + 30 เมนู ∞ โชว์เป็น 0 | U |
| **เทส LIFF กด + เกินสต็อก** | ⚠️ เมนูเดี่ยว ✅ กันได้ · **เซ็ต/คอร์ส ❌ ไม่เช็คสต็อกเลยสักบรรทัด** ← ต้นเหตุใบ U-0812-008 | U |
| **กิ๊ฟสร้างโค้ดส่วนลดไม่ได้** | ⏳ CC ตัดฝั่ง DB/RLS/ฟอร์มออกแล้ว (เขียนผ่าน 7/7 แบบ) · **ยังไม่รู้อาการจริง** ต้องถามกิ๊ฟ · เบาะแส: มีโค้ดแค่ 2 ตัว ทั้งคู่สร้าง 30 มิ.ย. = ไม่มีใครสร้างสำเร็จมา 6 สัปดาห์ | U + bug |
| **สลิปไม่ถูกแนบตอนสแกนจ่าย** | 🔴 **CC ยังไม่ได้ทำ** — นัทสั่งให้หา solution 1-2-3 เมื่อ 12 ส.ค. 18:41 แต่**คำสั่งหายในกะปัน** เพิ่งกู้มาได้ | **CC** |

---

## ③ เรื่องที่ CC เจอเพิ่มเอง — **นัทยังไม่เคาะ** ⚖️

1. **`SALMON` / `FREEWEEKDAY` ไม่มีอยู่ในตาราง `promo_codes` เลย** — เราสรุปกันว่า *"0 คนใช้เพราะกลไกผิด"* แต่ถ้าโค้ดไม่เคยถูกสร้าง ลูกค้าก็กรอกไม่ได้อยู่ดี → **ยืนยันก่อนยิง wave 2**
2. **สั่งเกินสต็อกที่ครัวนับ 15 เมนู** — หนักสุด `D064` ขาด 5 · `D199` ขาด 4 · `D123` ขาด 4 · `S025` ขาด 4
3. ~~**ชีทมีลแพลนของพลอย = จุดเดียวที่พังได้ทั้งก้อน**~~ → ✅ **import แล้ว 15 ส.ค.** (commit `นำประวัติ Meal Plan 10 เดือนจากชีทพลอยเข้าระบบ` · **1,656 รอบส่ง** จับคู่ด้วยเลขใบ Hato 99% · รันซ้ำได้ไม่ซ้ำ) — พี่ปืน verify จาก `origin/main` 16 ส.ค.
4. **วงเล็บ `( พี่นัท )` / `( พี่พลอย )` ในชีท = "คนดูแลลูกค้า" ใช่ไหม** — ถ้าใช่ ตอน import จะเก็บเป็นเจ้าของลูกค้าไปเลย (มีค่ากับ CRM)
5. **ระบบล็อกหลังบ้าน** — `app.360foodbox.com` + 3 ชั้น (เปิด / PIN ทีม / Gmail ส่วนตัว) · **ตอนนี้หน้าครัว-แอดมินไม่มีล็อกอะไรเลย** · เสนอทำ P2 (PIN) ก่อน
6. **`1A` ตั้งสต็อกค้างไว้ที่ 5** (ของจริง ∞) — เหลือจากการเทส รอนัทบอกให้คืน

---

## ④ ค้างเก่า — รอนัทคนเดียว 🔴

| | สถานะ |
|---|---|
| ช่อง Messaging API ของ OA | 🟡 **นัทยื่นขอคืนแล้ว 13 ส.ค.** รอ Hato ตอบ (CC ทวงทุก 1-2 วัน) |
| ใครไล่ทวงเงินค้าง | 🔴 **11 ใบ ฿13,766** |
| statement ธนาคาร (PDF) | 🔴 ค้างตั้งแต่ 27 ก.ค. · `finance/raw` ยังว่าง = ทำ P&L ไม่ได้เลย |
| สติกเกอร์ HT400 ขนาดไหน + เครื่องที่ 3 รุ่นอะไร | 🔴 |
| 9 เมนูติดธงสัปดาห์พิเศษ ยังไม่มีชื่อเล่น | 🔴 `D201 D150 D200 D054 D016 A18 D070 D162 D118` |
| หาคนครัวชายแทนอ่องคิน | 🔴 |
| แชท LINE OA ค้าง 11 ราย ใครตอบ | 🔴 |
| 3 คำถามการเงินใน `finance/ASK_NID.md` | 🔴 |

**✅ ปิดไปแล้ว:** DNS cutover (นัทกดเอง 12 ส.ค.) · ภาษาครัว ก/ข (เคาะ 11 ส.ค.)

---

## ⑤ ค้างเก่า — รอห้องอื่น 🟡 *(นัทไม่ต้องทำ)*

**U ถือ 7 งาน** — โค้ดส่วนลด · เซ็ตไม่เช็คสต็อก · หน้า DB อ่านค่ากลับ · ใบทวงเงิน · ใบสั่งผลิตครัว · N-37/38 เลือกเมนูเข้ารอบ · เลื่อนวันส่งใน OH
**ฟ้า** รายงาน 5 หัวข้อ (สั่งตั้งแต่ 12 ส.ค.) · **k** 2 งาน (บอทครัว · ทำฟ้าใช้ได้จริง) · **keng** แพลนส่ง+ตรวจ 3 ทาง · **rnd01** เซ็ต Hyrox ขึ้น LIFF

---

## ⑥ CC ถืออยู่ 🔵

สลิปไม่ถูกแนบ (เสนอ 1-2-3) · ยกออเดอร์ Hato ที่ขาด · เปลี่ยน 26 ไฟล์เป็น `app.360foodbox.com` · ซ่อนทางเข้าพนักงานในเว็บ · ทวง Hato ทุก 1-2 วัน · ใบออม 16 ส.ค. เลื่อนวัน · เทสพิมพ์จริงบน SUNMI · เตือนดาวน์เกรด Vercel (5 ก.ย.)

---

## ⑦ PM ถือ (เพิ่ม 13 ส.ค.)

| เรื่อง | ค้างตั้งแต่ | รออะไร |
|---|---|---|
| รอ 4 ห้องตอบ PM: **"เพิ่งรู้อะไรจากนัทที่ห้องอื่นยังไม่รู้"** (m · u · cc · kapan) | 13 ส.ค. | ห้องตอบกลับบอร์ด `pm` → PM รวบเข้าเอกสารกลาง |
| งานแก้ hook บอร์ด 3 ข้อ (ปิด claim อัตโนมัติ · เลิก fanout · `.claude/room`) | 10 ส.ค. | `cc` ยังไม่รายงานความคืบหน้า |
| ใบเช็คยอดประจำวัน `scripts/finance/daily_cash.mjs` | 10 ส.ค. | `f` ยังไม่ส่งใบแรก |

---

## ⑧ นัทแจ้งเอง 15 ส.ค. — ระบบรับเงินยังไม่ครบวงจร 🔴

**ปัญหา:** QR กับ โอนเข้าเลขบัญชี = การโอนเงินเหมือนกัน แต่**ปลายทางไม่เหมือนกัน** — ไม่มีที่แนบสลิปที่เดียวกัน
**นัทต้องการ 3 อย่าง:**
1. ทั้ง 2 ช่องทางไปจบที่ **"แนบสลิป"** ที่เดียวกัน
2. **แนบทีหลังได้** (ไม่ใช่ต้องแนบตอนสั่งเท่านั้น)
3. **แนบผ่านข้อความอัตโนมัติหลังสั่งซื้อในไลน์ได้** ← ผูกกับ `api/notify-order-confirm.js` ที่โค้ดเสร็จแล้ว

→ **นี่คือคำอธิบายว่าทำไมค้างจ่ายถึงเยอะ: ลูกค้าโอนแล้วแต่ไม่มีทางบอกเรา**
→ เจ้าของงาน: `u` (ไฟล์ `liff_customer.html` + `operation_hub.html`)

## ⑨ ไอเดียใหม่ 15 ส.ค. — ติดตามคนที่กำลังใช้ LIFF อยู่ 💡

ผูก `line_uid` เข้ากับ `live_presence` (ตอนนี้เก็บแค่ sid สุ่ม) + เก็บพฤติกรรมรายคน
→ รู้ว่าลูกค้าคนไหนชอบเข้ากี่โมง → broadcast รายคนได้ → **ก้าวแรกของ "เลขาส่วนตัวของลูกค้า"**
⚠️ ต้องมี PDPA consent ก่อนใช้จริง · รายละเอียด: `docs/VISION_FULL_LOOP.md`

---

## ⑩ พักไว้ 16 ส.ค. (skip turn)

| เรื่อง | ค้างตั้งแต่ | รออะไร |
|---|---|---|
| ~~**หาสาเหตุว่าทำไม `pi-puen-daily-sweep` ยิงตี 4 แล้วไม่ได้ผลลัพธ์**~~ | 16 ส.ค. | 🟢 **รันออกผลแล้ว 16 ส.ค.** → `docs/DAILY_BRIEF.md` + โพสต์บอร์ด `pm` 2 แถว · **แต่เจอข้อจำกัดจริง 1 ข้อ: รอบอัตโนมัติปลุกห้องด้วย `send_message` ไม่ได้** (ระบบบล็อก unattended session) → ขั้นที่ 3 ของ sweep ทำได้แค่โพสต์บอร์ด ห้องเห็นตอนเปิดครั้งถัดไป · เรื่อง "เคยเงียบ" ยังไม่ยืนยันว่าเป็น permission |
| **อ่านคำตอบเต็มของ 7 ห้องที่ตอบกลับคืน 15-16 ส.ค.** | 16 ส.ค. | pm เพิ่งเห็นบรรทัดแรก ยังไม่อ่านจบ |

---

## ⑪ พักไว้ 17 ส.ค. (skip turn · ห้องนิว)

| เรื่อง | ค้างตั้งแต่ | รออะไร / ใครตัดสิน |
|---|---|---|
| 🔴 **เคาะ: ทำเรื่องไหนก่อน — "รู้ใจลูกค้ารายคน" หรือ "สั่งวัตถุดิบอัตโนมัติ"** | 17 ส.ค. | **นัทเคาะ** · นิวแนะ *เรื่องลูกค้าก่อน* (ข้อมูลพร้อม ไม่ต้องรอใคร) · เรื่องวัตถุดิบต้องรอคนกรอกสูตร |
| 📊 **หลักฐานที่ขุดมาแล้ว (อย่าขุดซ้ำ):** สูตรใช้คำนวณวัตถุดิบได้จริง **18/128 เมนู (14%)** · มีสูตรแต่ยังไม่ใส่ของ 86 · ไม่มีสูตรเลย 24 → **"สั่งของอัตโนมัติ" ติดที่งานกรอกสูตร ไม่ใช่งานเขียนโค้ด** | 17 ส.ค. | — |
| 📊 **พิสูจน์แล้วว่า "รู้ใจลูกค้า" ทำได้เลย:** บ๊ะจ่างมีฤดูกาลชัด (พ.ค.–มิ.ย. ทุกปี · 2025-05 = 976 ลูก) + รู้ตัวลูกค้ารายคน (คุณเรย์ 40 · พิมพ์ณภัส 36 · K สุนันทา 26) จาก `order_items` 41,273 แถวที่มีอยู่แล้ว | 17 ส.ค. | — |
| **เส้นแบ่ง ฟ้า ↔ นิว — รอฟ้าตอบ 4 คำถาม** (ทำ MP อย่างเดียวไหม · งานสั่งวัตถุดิบครอบเมนูสต็อคด้วยไหม · มีงานอะไรอีกที่ยังไม่บรีฟ · เดดไลน์ศุกร์รับส่วนไหน) | 17 ส.ค. | **ห้องฟ้า** ยังไม่ตอบ |
| **บรีฟจากห้อง 05 เอิธ** (สถานะ · รอนัทกดอะไร · ผลล่าสุด) | 16 ส.ค. | **ห้อง 05** ยังไม่ตอบ |
| **push งานนิวขึ้น git** (`scripts/niw/` 3 ไฟล์ ยัง untracked) | 5 ส.ค. | **นัท** — ถามไป 4 รอบ ยังไม่ตอบ |
| **สั่งครัวตั้งสต็อก 6 เมนูที่ยัง "ไม่จำกัด"** → นิวจะเฝ้าได้ครบ 26/26 | 15 ส.ค. | **นัท → ครัว** |
| **เดดไลน์ศุกร์ 21 ส.ค.** (ใบสั่งของ + ใบผลิตพรุ่งนี้) — PM ส่งถึง K+ฟ้า+นิว เหมือนกัน 3 ห้อง | 16 ส.ค. | **ยังไม่มีใครแบ่งว่าใครทำส่วนไหน** |
| **3 เมนูไม่มีรูป** (`BJ5` ชื่อไม่ตรง Hato · `D197`/`D198` Hato ก็ไม่มี) | 5 ส.ค. | **นัท** — ถ่ายใหม่/หาโบรชัวร์ |

## ⑪ ค้างที่นัท — เติมกฎสิทธิ์ใน .claude/settings.json (19 ส.ค.)

| เรื่อง | ค้างตั้งแต่ | รออะไร |
|---|---|---|
| เติม 11 บรรทัดใน `.claude/settings.json` เพื่อให้ตัวตั้งเวลาพี่ปืนรันเองไม่ต้องรอนัทกดอนุมัติ | 19 ส.ค. | **นัทวางเอง** — Claude ถูกระบบบล็อกไม่ให้แก้ไฟล์สิทธิ์ตัวเอง (ลองแล้ว 2 ทาง Bash+Edit บล็อกทั้งคู่) · นัทเคาะว่า "เติมได้" แล้ว แต่ต้องวางมือ |

บรรทัดที่ต้องเติม (ต่อท้ายรายการ allow · อย่าลืมเติม , ท้ายบรรทัดเดิมก่อน):
`git fetch:*` `git log:*` `git status:*` `git branch:*` `git show:*` `git diff:*` `node scripts/*` `node -e:*` `curl -s:*` `curl -sS:*` `curl -sL:*`

---

## ⑫ พักไว้ 20 ส.ค. (skip turn · ห้องเลขา)

> 📌 **นัทสั่งย้ำเอง 20 ส.ค.:** *"ทุกครั้งที่ฉันชวนเปลี่ยนเรื่อง เรื่องเก่าที่ยังคุยค้าง กรุณาเก็บไว้ทุกครั้ง และชวนฉันกลับมาคุยเรื่องเดิมหากฉันไม่จบงานเก่าๆ ไม่งั้นมันค้าง"*
> → เลขาต้อง **เขียนลงไฟล์นี้ทันทีที่นัทเปลี่ยนเรื่อง** แล้ว **ยกกลับมาเองทีละเรื่อง** ห้ามรอนัทนึกได้

| เรื่อง | ค้างตั้งแต่ | รออะไร / ใครตัดสิน |
|---|---|---|
| **ส่งก้อน `CURRENT_FACTS` ให้นัทวางใน GPT** — เลขาถามว่า *"เอาแบบนี้ไหมครับ"* (BRAND_BIBLE+WORKFLOW วางครั้งเดียว · CURRENT_FACTS ส่งใหม่ทุกจันทร์) แล้วนัทเปลี่ยนเรื่องไป Omise | 20 ส.ค. | **นัทตอบคำเดียว** — เอา/ไม่เอา |
| **แยกบรีฟ GPT 4 ไฟล์ = ทำเสร็จแล้ว** (`docs/gpt/` + `scripts/gen_gpt_facts.mjs`) — commit `ebe66bd` | 20 ส.ค. | 🟢 ไม่ต้องทำอะไร แค่ให้รู้ |
| **จัดระเบียบอีเมล (K Plus → โฟลเดอร์แบงค์)** | 19 ส.ค. | ติดที่ยังไม่ได้เชื่อมบัญชี Gmail ของนัทเข้ากับเครื่องมือ |
| ~~เปิดรับบัตรเครดิต Omise~~ | — | ✅ **ปิดแล้ว 20 ส.ค.** — Omise ตอบว่า**เปิดใช้งานอยู่แล้วทั้ง 2 ช่องทาง** (บัตร 2.4%+VAT · PromptPay QR 1%+VAT) · เหลือแค่ **Payment Links+ อยู่ระหว่างตรวจสอบ ไม่ขอเอกสารเพิ่ม** จะแจ้งผลทางอีเมลอัตโนมัติ |

---

## ⑫ พักไว้ 20 ส.ค. — ห้องนิว skip มาลุยงานเจ (นัทสั่งเอง)

> *"ให้ห้องนิว เก็บสิ่งที่คุยค้างกับฉันอยู่ไว้ทั้งหมดใน todo แล้ว skip พวกนั้น มาเริ่มตรงนี้กับฉันก่อน"*
> ทุกข้อด้านล่าง = **หยุดไว้ ไม่ทำต่อ** จนกว่างานเจจะจบ (เคาะเมนู ~10 ก.ย.)

| เรื่อง | ค้างตรงไหน | รอใคร |
|---|---|---|
| **สลับเลข S/D ที่เหลือ** — ย้ายไปแล้ว 33/44 · เหลือ **7 ตัวที่กันไว้** (เป็นเมนูสัปดาห์ปัจจุบัน) + **4 ตัวติดหลังคิว** | รอสัปดาห์ปัจจุบันหมุนออก (หลัง 23 ส.ค.) แล้วรัน `scripts/niw/sd_apply.mjs` ซ้ำ | นิว (ทำเองได้) |
| **9 คู่ที่ชนกัน** (D011→D003 · D056/D158→D097 · D061/D144→D118 · D101→D134 · D147/D165→D167 · D153→D129) | ต้องแก้เป็นทอด — ยังไม่ได้วางลำดับ | นิว + นัทเคาะบางคู่ |
| **9 ตัวที่หาหัวไม่เจอ** — เสนอชื่อคล้ายไปแล้ว **นัทยังไม่ตอบ** · 2 ตัวน่าจะจบง่าย (`D148`→S020 · `D138`→S169 ทั้งคู่ 100%) | รอนัทเคาะ | **นัท** |
| **แพลนเมนู 2 สัปดาห์** (24-30 ส.ค. · 31 ส.ค.-6 ก.ย.) — เสนอไปแล้ว **ยังไม่ได้เซ็ตลง DB** · สัปดาห์ที่ 2 คู่ D ได้แค่ 2/5 | รอนัทเคาะ | **นัท** |
| **เคาะ: "รู้ใจลูกค้ารายคน" vs "สั่งวัตถุดิบอัตโนมัติ"** ทำอันไหนก่อน | park ตั้งแต่ 17 ส.ค. | **นัท** |
| **push `scripts/niw/` ขึ้น git** — ตอนนี้มี 8 ไฟล์แล้ว ยัง untracked ทั้งหมด | ถามไป 5 รอบ นัทตอบว่า "กลัวกระทบ" → ผมเลิกทวง | **นัท** |
| **เทมเพลตโบรชัวร์เมนูสัปดาห์** (เลขาส่งให้ 18 ส.ค. · `web/menu_brochure.html`) | ยังไม่ได้แตะเลย | นิว |
| **3 เมนูไม่มีรูป** (`BJ5` · `D197`/`D198`) | ค้างตั้งแต่ 5 ส.ค. | **นัท** |

✅ **เสร็จแล้วก่อน skip:** ย้ายเลข S/D 33 ตัว (สูตร+ตารางสัปดาห์อัปเดตตาม) · ตัวปิดเมนูหมดอัตโนมัติ 4 ทุ่มทุกวัน (scheduled task) · เปิดของเหลือสัปดาห์ที่แล้วคืน 4 เมนู · ติดแท็กสัปดาห์ D3/D4
