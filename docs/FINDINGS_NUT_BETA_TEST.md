# สิ่งที่นัทเจอระหว่างเทสเอง — รอกระจายงาน

> **กติกา (นัทสั่ง 6 ส.ค.):** ระหว่างเทส **ห้ามเพิ่มเรื่อง/ห้ามคุยแต่ละข้อ** — CC จดไว้เงียบๆ แล้วยิงทีเดียวตอนกระจายงาน
> ไฟล์นี้ = ที่ให้ของพวกนี้ไปตกอยู่ ไม่ใช่คิวงานที่ต้องทำทันที

---

## รอบเทส 6 ส.ค. 2026 (นัทเทส LIFF ด้วยตัวเอง)

### ✅ ผ่าน
- **ขั้น 1 — ใส่ตะกร้า:** กดใส่ได้ ตัวเลขบนตะกร้าขึ้นถูก

### 📝 ที่ติ (ยังไม่วิเคราะห์ ยังไม่ส่งใคร)

| # | เรื่อง | หน้าจอ |
|---|---|---|
| N-01 | **กดสั่งได้ไม่จำกัด** — สต็อกไม่บล็อกตอนกดเพิ่ม | LIFF รายการเมนู |
| N-02 | **ป้าย "เมนูพิเศษสัปดาห์นี้" เป็นแถบสีเหลือง และไม่บอกเลขสัปดาห์** | LIFF รายการเมนู |
| N-03 | **หลายเมนูยังไม่มีแถบ/เลขสัปดาห์** — ที่ควรมีดาวคือเมนูประจำเท่านั้น (ตัวอย่างที่นัทวงไว้: `S012` ข้าวแซลมอนซอสกะเพราเห็ดออรินจิ) | LIFF รายการเมนู |
| N-04 | **รูปการ์ด Diet Set ไม่สวย อยากเปลี่ยน** | LIFF หน้าแรก การ์ดโปรโมชั่น |
| N-05 | **ไม่แน่ใจว่าเซ็ตต่างๆ มาครบไหม** — ต้องเทียบกับลิสต์แอดมิน | LIFF หน้าแรก การ์ดเซ็ต |
| N-06 | **เซ็ตที่เลือกเมนูซ้ำกัน ควรรวบเป็นบรรทัดเดียว + บอกจำนวน** — ตอนนี้ Diet Set A ขึ้น "เส้นหมี่น้ำยาทูน่า" ซ้ำ 12 บรรทัด | LIFF ตะกร้า/checkout |
| N-07 | **ปุ่ม "เปลี่ยน" (สีแดง) จริงๆ คือลบทิ้งเลย** → ควรใช้คำว่า "ลบ" | LIFF ตะกร้า/checkout |
| N-08 | **ไม่เลือกวิธีชำระเงินก็กดสั่งได้** → ต้องมีระบบทวงเงินลูกค้า | LIFF checkout |
| N-09 | **ยังไม่มี QR code โอนเงินจริง** (ช่อง QR/PromptPay เป็นรูป placeholder) | LIFF checkout |
| N-10 | **ค่าส่งรู้สึกถูกไป** — ปักหมุด 4.8 กม. คิด ฿29 (ยังไม่ได้ทดสอบกรอกโค้ดส่วนลด) | LIFF checkout |

| N-11 | **คำอธิบายโค้ดส่วนลดขึ้นเป็น `????`** — ภาษาไทยเพี้ยน (ตัวเลขลดถูก ฿50 แต่ข้อความอ่านไม่ออก) | LIFF checkout · ช่องคูปอง |
| N-12 | **กด "สั่งเพิ่มอีกครั้ง" กลับหน้าแรก — รายการยังโชว์ว่าติ๊กไว้ แต่ตะกร้าว่าง** (สถานะไม่ตรงกัน) | LIFF หน้าแรก |

| N-13 | **หน้าครัวยาวเกินไป** — อยากกดชื่อลูกค้าแล้วพับเก็บ (collapse) ได้ | KQ |
| N-14 | **แท็บ "รายสินค้า" ไม่ได้ประโยชน์** — ครัวยังไม่รู้ว่าต้องผลิตเมนูอะไรเท่าไหร่ · อยากให้สรุปเป็น **ใบสั่งผลิต** (เมนู × จำนวน) | KQ แท็บรายสินค้า |
| N-15 | **หน้าออเดอร์หาใบยากมาก — ไม่มีปุ่มค้นหา** แอดมินต้องหาลูกค้าเจอ | OH หน้าออเดอร์ |
| N-16 | **หน้าแมสรายการยาวพรืด ดูยาก** | OH แมสเซนเจอร์ |
| N-17 | **Lalamove จัดรอบไม่แบ่งเช้า/บ่าย** — `U-0807-004` เป็นออเดอร์เช้า (8:00-13:00) แต่ไปอยู่รอบที่ 10 ท้ายสุด ทั้งที่รอบ 1-9 เป็นบ่าย | OH แมส · กล่องยิง Lalamove |
| N-18 | **กล่อง "สะสมมาแล้ว 2 วัน" ควรพับเก็บได้ + ตั้งชื่อหัวข้อว่า "data ประหยัดเงิน"** | OH แมส |
| N-19 | 🔴 **ออเดอร์ที่ลูกค้าไม่ปักหมุด จะไม่เข้าหน้าแมสเลย (พิกัดว่าง)** → แอดมินต้องไล่ถามที่อยู่ + เก็บเงินค่าส่งย้อนหลัง<br>**นัทเสนอ: บังคับปักหมุด + ตอนกรอกพิกัดให้ลูกค้าแชร์ current location** | LIFF checkout → OH แมส |
| N-20 | ที่อยู่ที่เลือกจาก autocomplete ("ซุ้ม เพชรภูเงิน") — นัทจำไม่ได้ว่ากดเองไหม ควรมีวิธียืนยันให้ชัด | LIFF checkout |

| N-21 | **เมนู Request ในหน้า Meal Plan — ขอทำเป็นแบบไม่มีรูปไปก่อน** (ตอนนี้เป็นกล่องรูป placeholder → ลูกค้าจะทวงหารูป) | LIFF · Meal Plan picker |
| N-22 | **ยังไม่มีเซ็ต Hyrox ให้กด** | LIFF · หน้าแรก/เซ็ต |

| N-25 | **หน้าสั่งสำเร็จของ Meal Plan ควรบอก "รอบต่อไปส่งวันที่เท่าไหร่"** + ปุ่ม "แก้ไขวันส่งรอบต่อไป" | LIFF หน้าสำเร็จ |
| N-26 | 🔴 **Meal Plan โดนคิดค่าส่ง ฿17** — `U-0807-005` ยอด ฿4,207 = 4,190 + 17 · แต่นโยบายร้านคือ **MP ส่งฟรีทุกแพ็ค ไม่มีขั้นต่ำ** (CC เจอเอง ไม่ใช่นัทติ) | LIFF checkout · ค่าส่ง |

| N-27 | 🔴🔴 **รอบ 2-3 ของ Meal Plan ไม่มีใบในหน้าออเดอร์และหน้าแมส** — **แมสไม่รู้ว่าวันที่ 10/12 ต้องไปส่งให้ใคร**<br>**verify กับ DB แล้ว:** `U-0807-005` มี `mp_deliveries` ครบ 3 รอบ (7·10·12 ส.ค.) แต่มี `orders` แค่ใบเดียว (7 ส.ค.)<br>⚠️ **ไม่เหมือนกันในระบบ:** ลูกค้า MP ที่ย้ายมาจาก Hato มีใบทุกรอบ (`MP-0810-015..020`) แต่คนสั่งใหม่ผ่าน LIFF ได้ใบเดียว → **หลัง cutover ทุกคนสั่งผ่าน LIFF = เจอหมด** | LIFF submit · OH แมส |
| N-28 | **หน้าแพลนเมนูไม่โชว์เลขออเดอร์** — ตามกลับไปหาใบต้นทางไม่ได้ เสี่ยงจับผิดคน | OH แพลนเมนู |
| N-29 | **ลิสต์เมนูฝั่งซ้ายในแพลนเมนู — ไม่ต้องมีช่องค้นหาแล้ว** ให้แสดงทุกเมนูไปเลย scroll หาเอา (นัทเปลี่ยนใจ) | OH แพลนเมนู |
| N-30 | **กล่องสรุปฝั่งซ้าย ควรมี toggle "ดูเฉพาะเมนูที่ต้องผลิตวันนั้น"** — ซ่อนเมนูที่ไม่ได้ถูกติ๊กทิ้ง | OH แพลนเมนู |
| N-31 | **เมนู LC/HP ที่เพิ่มใหม่ใน DB ต้องไปโผล่ในลิสต์ "เมนู Request" ด้วย** — แต่ถ้ามาหมดจะรก → **ช่องค้นหาในลิสต์ Request เก็บไว้ดีแล้ว** | OH แพลนเมนู · LIFF Request |

| N-32 | 🔴 **หน้าครัวดูวันล่วงหน้าไม่ได้** — ปฏิทินมีให้เลือกแต่วันย้อนหลัง (6 ส.ค. ถอยไป 24 ก.ค.) เลือก 10 หรือ 12 ส.ค. ไม่ได้ → **ครัวเตรียมของล่วงหน้าไม่ได้** และตรวจไม่ได้ว่ารอบ MP วันนั้นมีของไหม | KQ · ตัวเลือกวันที่ |
| N-33 | 🔴🔴 **แพลนเมนูแล้ว แต่ครัวไม่เห็นเมนู** — `U-0807-005` ในหน้าครัวขึ้นแค่ `MP-HP-W Meal Plan HP · 1 สัปดาห์ (21 กล่อง) ×1` ไม่ใช่เมนู 7 กล่องที่เพิ่งแพลน<br>**= บั๊กเดียวกับที่ห้อง P ไล่เจอ** (ออเดอร์มี `order_items` อยู่แล้ว → fallback ของ KQ ไม่ยิง → ครัวเห็นของเก่า/ของผิด)<br>✅ **ตอนนี้มีเคสจริงให้ U ทดสอบซ้ำได้: `U-0807-005`** | OH แพลนเมนู → KQ |

---

## รอบเดโม่กับแอดมิน 7 ส.ค. 2026 (นัทสั่ง: "จดก่อน อย่าเพิ่งแก้")

| # | เรื่อง | หน้าจอ |
|---|---|---|
| N-34 | **ปุ่ม "+ เพิ่มกลุ่ม" มองไม่เห็น** — อยู่มุมขวาบนกล่องส้ม ไกลจากข้อความ "ยังไม่มีกลุ่ม" ที่อยู่ตรงกลาง → แอดมินหาไม่เจอ · ควรมีปุ่มใหญ่ตรงข้อความนั้นเลย | DB → แพคเกจ |
| N-35 | **สร้างแพคเกจแล้วไม่มีอะไรเตือนว่ายังใช้ไม่ได้** — บันทึกได้ เปิดขายได้ ลูกค้าเห็นการ์ด แต่กดเข้าไปเลือกอะไรไม่ได้ (กลุ่มว่าง) · ควรขึ้นป้ายแดง "ลูกค้าสั่งไม่ได้ — ยังไม่มีกลุ่มการเลือก" และกันไม่ให้ติ๊กเปิดขาย · **ของจริงที่ค้างอยู่ตอนนี้: แพค "เทส ทดลอง ฿500" เปิดขายอยู่ กลุ่มว่าง** | DB → แพคเกจ |
| N-36 | **ลิสต์ "เมนู + ราคาบวกเพิ่ม" โชว์ 961 รายการ รวมสูตรที่ยังไม่มีชื่อ (ขึ้นเป็น "–")** — เมนูขายจริงมี 118 · แอดมินติ๊กของไม่มีชื่อไปแล้ว 12 รายการโดยไม่รู้ตัว · ควรโชว์เฉพาะเมนูที่เปิดขายจริง | DB → แพคเกจ |
| N-37 | **เซ็ตแบ่งส่ง — ลูกค้าไม่รู้ว่าของอะไรไปรอบไหน** · หน้าเช็คเอาท์บอกแค่ "รอบ 1 · 21 กล่อง" / "รอบ 2 · 21 กล่อง" แต่ไม่บอกว่าเมนูไหนส่งก่อน เมนูไหนส่งทีหลัง → ลูกค้าวางแผนกินไม่ได้ และแอดมินตอบคำถามนี้ไม่ได้ | LIFF checkout · เซ็ตแบ่งรอบ |
| N-38 | **ยังไม่เคาะ: ใครเป็นคนเลือกว่าเมนูไหนไปรอบไหน** — ระบบแบ่งให้เอง (เท่าๆ กัน) vs **ให้ลูกค้าเลือกเองว่าจะเอาอะไรรอบ 1 อะไรรอบ 2** · นัทตั้งเป็นประเด็นไว้ ยังไม่ตัดสิน · ⚠️ กระทบทั้งหน้าเช็คเอาท์ · การแตกใบออเดอร์ · และใบงานครัว | LIFF checkout · เซ็ตแบ่งรอบ |

| N-39 | **หน้าโปรโมชั่น — อยากให้แก้ไขโค้ดได้ทุกตัว** · ตอนนี้ปุ่ม "แก้ไข" โผล่เฉพาะโค้ดที่ **ยังไม่มีใครใช้** (`used_count = 0`) · โค้ดที่ถูกใช้ไปแล้วแก้ไม่ได้เลย มีแต่ปุ่ม เปิด/ลบ (เช่น `UNDER50` ใช้แล้ว 1 ครั้ง → ไม่มีปุ่มแก้ไข ส่วน `JULY100`/`FREESHIP` ใช้ 0 ครั้ง → แก้ได้)<br>⚠️ กติกาเดิมตั้งใจกันแก้ย้อนหลังแล้วยอดที่เคยลดไปเพี้ยน — ถ้าจะเปิดให้แก้ ต้องคุยว่าแก้ได้แค่ไหน (เช่น แก้คำอธิบาย/วันหมดอายุได้ แต่ห้ามแก้มูลค่าส่วนลด) | OH → โปรโมชั่น |

| N-40 | **หน้าประวัติออเดอร์ของลูกค้า — เมนูซ้ำขึ้นทีละบรรทัด ยาวเป็นหน้าจอ** · ต้องรวมเป็นบรรทัดเดียวแล้วบอกจำนวนรวม (เช่น `เส้นหมี่น้ำยาทูน่า × 18`)<br>ตอนอยู่แพคเกจยิ่งหนัก — เห็นบรรทัดเดียวกันซ้ำ 18 บรรทัดติดกัน แต่ละบรรทัดขึ้น ฿0<br>⚠️ **ในจอเดียวกันมี 2 แบบปนกัน**: `U-0808-003` รวมให้แล้ว (`× 18`) แต่ `U-0810-001` ไม่รวม → คนละเส้นทางโค้ด<br>*(คนละหน้ากับ N-06 ที่แก้ไปแล้ว — อันนั้นคือตะกร้า/เช็คเอาท์ อันนี้คือหน้าประวัติออเดอร์)* | LIFF → ดูออเดอร์ทั้งหมด |

| N-41 | 🔴 **ตารางข้อจำกัดอาหาร (`customer_preferences`) ว่างเปล่า 0 แถว** — ระบบมีที่เก็บและมีช่องกรอกครบแล้ว (OH → ข้อมูลลูกค้า → "แพ้" / "ไม่ทาน" · และ LIFF โปรไฟล์ให้ลูกค้ากรอกเอง) **แต่ไม่มีใครเคยกรอกเลยสักคน**<br>ของจริง: ร้านรู้อยู่แล้วว่าใครไม่กินเนื้อ/แพ้กุ้ง แต่อยู่ในหัวแอดมิน ไม่ได้อยู่ในระบบ → **แพลนเมนูจึงกันเมนูแพ้ให้ไม่ได้จริง และน้องนิวก็ใช้ไม่ได้**<br>→ ต้องมีรอบ "แอดมินกรอกย้อนหลัง" ให้ลูกค้า MP ที่มีอยู่ก่อน | OH ข้อมูลลูกค้า · LIFF โปรไฟล์ |
| N-42 | **หน้าแพลนเมนูไม่มีช่องกรอกข้อจำกัดอาหารตรงนั้น** — นัทหาไม่เจอตอนใช้งานจริง (ต้องออกไปหน้าข้อมูลลูกค้าก่อน แล้วค่อยกลับมา) · จุดที่คนคิดเรื่องนี้คือตอนกำลังจัดเมนูให้ลูกค้า → ควรกรอก/แก้ได้ตรงนั้นเลย หรืออย่างน้อยโชว์ค้างไว้ข้างชื่อลูกค้า | OH → แพลนเมนู |

### 💡 ไอเดียอนาคต (park ไว้ ไม่ใช่งาน cutover)
- **toggle "รับแช่เย็นได้"** ในหน้า Meal Plan — อธิบายว่าปกติทำเสร็จส่งเลย (ได้ของเย็น) แต่ถ้าลูกค้ารับแบบแช่ค้างคืนได้ → **ปลดวันส่งเพิ่ม** (เช่น 7 → 8) และ **มีช่วงเช้าโผล่มาให้เลือก** ในวันถัดไป
- **UX เลือกวัน Meal Plan:** ให้ลูกค้าเลือกวันแรกวันเดียว แล้วระบบขึ้นบอกว่า "รอบต่อไปจะส่ง 10 และ 12" (แทนที่จะให้เลือกทั้ง 3 วันเอง) — นัทยังไม่เคาะว่าเอาแบบไหน
- หน้า KQ ทำ **สรุปใบสั่งผลิตประจำวัน** (เมนูไหน กี่กล่อง) — โยงกับงานหน้า R&D ที่นัทกำลังคิด

### ✅ ผ่านเพิ่ม (ขั้น 2-3)
- ตะกร้า: รายการตรง · มีตัวเลขจำนวน · กดลบออกได้ · ยอดลดตาม
- เลือกวันส่ง: กดได้ทุกวัน · **คอร์ส/Meal Plan ขึ้นสีเทาถูกต้อง** · เลือกมะรืนได้
- ช่วงเวลา: มีครบ 3 ช่วง (รับได้ตลอดวัน / เช้า / บ่าย)
- ปักหมุด: แผนที่ทำงาน · คำนวณระยะขับจริง 4.8 กม. → ค่าส่ง ฿29

---

*อัปเดตล่าสุด: 6 ส.ค. 2026 · CC จดระหว่างนัทเทส*
