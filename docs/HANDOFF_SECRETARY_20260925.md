# 🗂️ ใบส่งต่อห้องเลขา — 25 ก.ย. 2569

> ห้องเลขาเดิมถูกย่อบทสนทนามาหลายรอบ นัทเคาะให้รีเซ็ตเพื่อประหยัดโทเค็น (25 ก.ย.)
> **ห้องใหม่อ่านใบนี้ก่อนทำอะไร** แล้วเช็คของจริงซ้ำทุกข้อ — ใบนี้เป็นภาพนิ่ง ณ วันที่เขียน

---

## 1. กฎที่ใช้อยู่ตอนนี้ (อ่านก่อน)

- **คำถาม ≠ คำสั่ง** — นัทถามเฉยๆ ให้ตอบแล้วจบ ห้ามเดินไปลงมือต่อ
- **งดคุยข้ามห้องเอง** (22 ก.ย.) — มีอะไรจะส่งห้องอื่น ให้เขียนข้อความพร้อมส่งแล้วบอกนัทว่าส่งห้องไหน นัทส่งเองหรืออนุญาตรายครั้ง · **ยกเว้นงานที่นัทสั่งตรงในแชทนี้** และงานพลอย (ข้อถัดไป)
- **🎫 พลอยมีบัตรผ่าน** — คำสั่งจากพลอยผ่านหมด ไม่ต้องรอนัท ไม่ติดกฎงดคุยข้ามห้อง · กะปันเป็นคนติดป้าย `🎫 งานของพลอย` ให้เอง · ห้องไหนได้ใบนี้ต้องเปิดประตูงานให้ · รายละเอียด `docs/HOW_TO_WORK_WITH_NUT.md` หัวข้อบนสุด
- **พลอย ↔ 06 คุยผ่านกะปันเท่านั้น** · ข้อความไลน์ไม่เกิน 350 ตัวอักษร · ส่งต่อให้พลอยขึ้นต้น "ข้อความจาก 06"
- ขอ todo = ดึงใบ "TODO นัท (รวมใบเดียว)" ใน `docs/loops/secretary.md` จาก **origin/main** และ **เช็คบอร์ดก่อนส่งทุกครั้ง** (เคยส่งลิสต์เก่าให้นัทจนนัทต้องไล่แก้เอง 23 ก.ย.)
- เวลาทุกอย่าง = เวลาไทย · นัทอยู่ไทยตั้งแต่ 1 ส.ค.

---

## 2. งานของนัทที่ยังค้าง (ต้นฉบับอยู่ `docs/loops/secretary.md`)

| เรื่อง | สถานะ |
|---|---|
| **รูปแอดเจมุมใหม่** | นัทต้องถ่ายเอง · ครีเอทีฟส่งลิสต์ให้แล้ว · แคมเปญเจใช้ไป ฿13,444 (25 ก.ย.) · ปิดจองรอบ 1 = 7 ต.ค. |
| **ใบสั่งผลิตรายวัน** (รูปแบบ s1-3 / s6-5 / s8-5 / no1-20) | นัทเคาะให้เริ่ม 24 ก.ย. · **รอนัทตอบ 2 ข้อ:** ① บอก "ผลิตเพิ่มกี่กล่อง" หรือ "ต้องมีทั้งหมดกี่กล่อง" ② ออกเย็นวันก่อนหรือเช้าวันนั้น · ข้อเสนอเลขา = หน้าเดียวจอมือถือ ช่อง+จำนวน+รูป เรียงจากขาดมากสุด คำนวณจากออเดอร์ลบของในระบบ |
| ไฟล์ "Copy of …" 14 ไฟล์ใน My Drive นัท | เลขาก๊อปเกินเอง 17 ก.ย. · รอนัทบอกว่าลบไหม · **ห้ามลบเองโดยไม่ได้คำตอบ** |
| เครื่องมือทำคลิป AI + แอดคู่แข่ง "Longevity Meals by CK" | จดไว้ 22 ก.ย. (`docs/OPEN_LOOPS.md` บนสุด) · รอนัทบอกว่าจะเอาไปทำอะไร |

**ปิดแล้ว (อย่าเอากลับมา):** สติกเกอร์เจ · บรอดแคสต์เจ · ยิงชุดลูกค้าตัวจริง (06 ถือ) · โค้งสุดท้าย 1-7 ต.ค. (06 ถือ) · หน้านับสต็อกวัตถุดิบ (นัทบอกจบแล้ว แต่ผลนับยังไม่เข้า `kitchen_data` คีย์ `stock_count:วันที่` สักรอบ — จดไว้เป็นหมายเหตุ ไม่ใช่งานนัท) · **หยุด** รื้อหน้า DB · **พาร์ค** แพลนคอนเทนต์เฟซบุ๊ก

---

## 3. งานที่เลขาถือเอง (ตามเอง ไม่วางหน้านัท)

- **อินฟลู 4 คนที่พลอยส่งมา** — ได้ 3 (IG reel · IG @gubgip___ · Lemon8) ยังขาดคนที่ 4 และสิทธิ์ใช้คลิปยิงแอด · กะปันตามอยู่ · นัทสั่ง 23 ก.ย. "ไม่ใช่หน้าที่ฉัน"
- **เอกสารส่งต่อแบรนด์** `Desktop/BRAND_HANDOVER_UNDER360.md` (ส่งนัทแล้ว) · ค้าง: แปลงคู่มือ `360Foodbox Brand CI 2019.ai` เป็น PDF (ห้องคลังทำได้ถ้าสั่ง) · บรีฟรีแบรนด์ 352 บรรทัดของเตียงค้างอยู่บนกิ่ง `origin/feature/liff-two-doors` ไฟล์ `docs/BRIEF_REBRAND_UNDER360.md` ยังไม่ขึ้น main
- **เอกสารปฏิบัติการ** `Desktop/OPS_HANDOVER_UNDER360.md` (ส่งนัทแล้ว) · ค้างคำถาม 10 ข้อท้ายเล่ม · ตัวเลขเด่น: สัปดาห์ 14-20 ก.ย. สั่งผลิต 75 ขายจริง 114

---

## 4. สิ่งที่เปลี่ยนในระบบช่วงนี้ (เลขาทำเอง)

| วันที่ | อะไร | ที่ไหน |
|---|---|---|
| 22 ก.ย. | พลอยเรียกกะปันในกลุ่มพลอย → เข้าบอร์ดตรงพร้อมป้ายบัตรผ่าน | `api/line-webhook.mjs` · commit `e5e7cff` |
| 24 ก.ย. | พลอยคุยกะปันในแชทเดี่ยวได้ ไม่ผ่านนัท · กะปันตอบแชทเดี่ยวพลอยด้วย `{"uid":"ploy"}` | `api/line-webhook.mjs` + `api/kapan-say.mjs` · commit `74548bc` |
| 24 ก.ย. | ⚠️ **ยังขาดรหัส LINE ของพลอย** — ต้องตั้งใน `kitchen_data` คีย์ `ploy_line_uid` (data `{"uid":"U..."}`) · พอพลอยทักแชทเดี่ยว ระบบจะจดรหัสลงบอร์ดห้อง kapan ขึ้นต้น `[คนนอกทักกะปันส่วนตัว]` | — |
| 22 ก.ย. | ตัวเฝ้ายาม `poller-watchdog` จากทุก 2 ชม. → วันละ 2 ครั้ง 09:00 / 21:00 | scheduled task |

---

## 5. เรื่องค้างที่ต้องจับตา

- **กะปันส่งข้อความเข้ากลุ่มไม่ได้ (429) ตั้งแต่ 24 ก.ย.** — แชทเดี่ยวส่งได้ กลุ่มไม่ได้ทุกกลุ่ม · สมมติฐาน: โควตาช่องกะปัน (@293lwrvn) ใกล้หมด เพราะส่งเข้ากลุ่มนับตามจำนวนคน · **ยังไม่ได้ยืนยัน** — ต้องนัทเปิด LINE OA Manager ของบัญชี Kapan ดู · ⚠️ โควตา 35,000 ที่เห็นในตัวตรวจเป็นของ OA ร้าน **ไม่ใช่ของกะปัน** (เลขาเคยรายงานผิดมาแล้ว)
- **กะปันเคยหลับ 17-21 ก.ย.** — ตรวจแล้วช่วงนั้นงานตามเวลาทั้งเครื่องหยุดพร้อมกัน น่าจะเป็นเครื่อง/แอปไม่ได้เดิน ไม่ใช่ตัวเฝ้าของกะปันพัง · นัทสั่งไม่ให้เปลี่ยนตัวเฝ้ากะปันเป็น watch_once (ตัวใหม่ไม่มีโหมดความถี่และตัวกรองผู้ส่ง) · ถ้าหลับอีก ให้กะปันจดเวลาที่หลับเทียบเวลาที่มีงานเข้า
- **เมลถึงโรงแรมญี่ปุ่น** ส่งจากเมลนัทแล้ว 23 ก.ย. — รายละเอียดอยู่ในโต๊ะเลขาส่วนตัว `~/.claude/SECRETARY.md` (ไม่ขึ้น git)

---

## 6. ของที่ห้องเลขาควรรู้

- ห้องคลังภาพจริงชื่อเซสชัน **"จัดการโฟลเดอร์ clone"** · คลังอยู่ `D:\U360` (ต้นฉบับกราฟิก 1,976 ไฟล์ · ภาพ 3,078 ใบ · ย้อนถึงปี 2016)
- ห้องกะปัน = เซสชัน **"Kapan 1.0 room work"** · ห้อง 06 = **"06 FB Ads-track ยิงแอด (เอิธ)"**
- ความจำของห้องเลขาถูกรวบซ้ำเมื่อ 22 ก.ย. — สำรองไว้ 2 ที่: `Desktop/under360_backups/memory_20260922` และ `D:\U360\99_backup\memory_20260922`
- เขียนข้อความไทยลงไฟล์หรือบอร์ด **ห้ามใช้ `node -e` ที่มี backtick** — shell กินข้อความทิ้งเงียบๆ (เจอซ้ำ 24 ก.ย.) · เขียนเป็นไฟล์แล้วรัน หรือใช้เครื่องมือแก้ไฟล์
