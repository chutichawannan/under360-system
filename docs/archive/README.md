# 🗄️ archive — เอกสารที่หมดอายุแล้ว (ไม่ได้ลบ แค่ย้ายออกจากทาง)

> จัดเมื่อ **15 ส.ค. 2026** (นัทสั่ง · ระดับ "เบา" = ย้ายอย่างเดียว ไม่ลบอะไรเลย)
> **ทุกไฟล์ในนี้ยังอ่านได้ปกติ** — แค่ไม่ได้อยู่ในสายตาตอนทำงานประจำวัน

---

## 📦 มีอะไรอยู่ในนี้

| โฟลเดอร์ | มีอะไร | ทำไมย้ายมา |
|---|---|---|
| `kitchen_daily/` | ใบงานครัวรายวัน `FAH_20260810*` `FAH_20260811*` (5 ไฟล์) | **ผ่านวันไปแล้ว** — ใบใหม่ออกทุกวัน ของเก่าไม่ได้ใช้ซ้ำ |
| `handoff/` | งานส่งมอบที่จบแล้ว (8 ไฟล์) — `HANDOFF_07/08/09_UTRACK` · `HANDOFF_CC_20260805` · `HANDOFF_P_20260808` · `CLAUDE_CODE_HANDOFF` · `MKT_HANDOFF` · `STATE_20260805` | งานส่งมอบจบแล้ว · `STATE_20260805` เป็น snapshot ตัวเลขเก่า (เมนู 118 ฯลฯ) **ที่ตอนนี้ไม่ตรงแล้ว** |
| `migration/` | งานย้ายบ้านที่เสร็จแล้ว (4 ไฟล์) — `WIX_SEO_HARVEST` · `WEB_LAUNCH_TODO` · `PARALLEL_HATO_SYNC_BRIEF` · `DNS_CUTOVER_VALUES` | **DNS ย้ายเสร็จ 12 ส.ค.** · Hato parallel sync จบแล้ว |
| `social/` | ชีทโซเชียลสัปดาห์ 6-12 ส.ค. | ผ่านสัปดาห์ไปแล้ว (ใบ 13-19 ส.ค. ยังอยู่ที่ `web/tiang/`) |

**รวม 18 ไฟล์ · ~180 KB**

---

## ⚠️ เอกสารที่ยังอ้างถึงไฟล์เหล่านี้อยู่

`CLAUDE.md` ยังมีข้อความอ้างชื่อไฟล์พวกนี้อยู่หลายจุด **แต่เป็นการเล่าประวัติ ไม่ใช่ลิงก์ที่ต้องกดตาม** → **ไม่ได้แก้ CLAUDE.md** เพราะทุกห้องใช้ไฟล์นั้นร่วมกัน แก้แล้วเสี่ยงชน

**ถ้าเจอเอกสารไหนอ้างชื่อพวกนี้แล้วหาไม่เจอ → มันอยู่ที่นี่** `docs/archive/<หมวด>/<ชื่อไฟล์>`

🔴 **จุดที่ต้องระวังเป็นพิเศษ:** `CLAUDE.md` เคยเขียนว่า *"`docs/STATE_20260805.md` = เอกสารความจริงกลาง ทุกห้องอ่านก่อนทำงาน"*
→ **ประโยคนั้นหมดอายุแล้ว** ตัวเลขในไฟล์นั้นเก่ากว่าปัจจุบัน 10 วัน · **ความจริงกลางตอนนี้ = query จาก DB จริง + `origin/main`** ไม่ใช่ snapshot ไหนทั้งสิ้น

---

## ↩️ วิธีถอยกลับ (ถ้าอะไรพัง)

### ทางที่ 1 — ถอยด้วย git (สำหรับไฟล์ที่อยู่ใน git)
```bash
git revert <commit ของการย้ายครั้งนี้>
```
ไฟล์ทุกตัวจะกลับไปที่เดิมทันที · **ประวัติไม่หาย เพราะใช้ `git mv` ไม่ใช่ลบแล้วสร้างใหม่**

### ทางที่ 2 — ถอยจากไฟล์สำรอง (ครอบคลุมไฟล์ที่ไม่ได้อยู่ใน git ด้วย)
สำรองเต็มไว้ก่อนย้าย **57 ไฟล์ · 1.5 MB** ที่:
```
C:\Users\PP\Desktop\under360-backup-md-20260815\
```
- `docs/` — สำเนาโฟลเดอร์ docs ทั้งอัน ณ ก่อนย้าย
- `root/` — ไฟล์ .md ที่อยู่ root
- `web_tiang/` — ชีทโซเชียล
- `COMMIT_BEFORE.txt` — commit ที่ยืนอยู่ก่อนย้าย (`b9ca5d6`)

**ก๊อปกลับทับได้เลย** ถ้าต้องการของเดิมเป๊ะ

⚠️ **ไฟล์ที่ต้องพึ่งทางที่ 2 เท่านั้น** (เพราะไม่เคยอยู่ใน git): ใบงานครัว `FAH_*` ทั้ง 5 · `CLAUDE_CODE_HANDOFF.md` · `DNS_CUTOVER_VALUES.md` · `HANDOFF_P_20260808.md`

---

## 📏 กติกาต่อจากนี้ (กันรกซ้ำ)

1. **ไฟล์ที่มีวันที่ในชื่อ = ของชั่วคราว** → เกิน 7 วันย้ายเข้า `archive/`
2. **handoff = ของชั่วคราว** → งานส่งมอบจบเมื่อไหร่ ย้ายเมื่อนั้น
3. 🔑 **ความรู้ถาวรห้ามค้างอยู่ในไฟล์ที่มีวันที่** — เจอบทเรียนดีๆ ใน handoff ให้**ยกขึ้นไป `CLAUDE.md` หรือ `docs/` ตัวหลักก่อน** แล้วค่อยย้าย
4. **1 เรื่อง = 1 ไฟล์** — อย่าสร้างไฟล์ใหม่ถ้ามีไฟล์ที่ครอบเรื่องนั้นอยู่แล้ว

📄 สารบัญเอกสารทั้งหมด: `docs/DOC_INDEX.md`
