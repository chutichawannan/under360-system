# HANDOFF → 09 U-track · FINAL BETA
> ส่งต่อจาก session 13 (5 ส.ค. 2026) · **cutover Hato 7 ส.ค. = เหลือ 2 วัน**
> ตัวเลขทุกตัวในไฟล์นี้ **query สดจาก Supabase 5 ส.ค.** — ถ้าเอกสารอื่นขัดกัน ให้ verify เองก่อนเชื่อ

## อ่านก่อนเริ่ม (ตามลำดับ)
1. skill `under360-session-start` → `CLAUDE.md`
2. `TODO.md` (repo root) — หัวข้อ 🔴 นัทต้องทำ
3. ไฟล์นี้
4. **verify ตัวเลขเองก่อนรายงานอะไรทั้งนั้น** (ดูหัวข้อ "กับดัก" ข้อ 1)

---

## 🎯 scope beta ที่นัทปรับแล้ว (สำคัญ — อย่าทำเกิน)
> นัท: *"ยังไม่ได้ทดลองสิ่งที่ไปถึงครัวเลย · ถ้า beta แล้วจะทยอยแก้งานครัว · ตอนนี้เอาแค่ **ลูกค้าสั่ง → สินค้าเข้าระบบ → มีตารางแมสให้เห็น** ก็พอ"*

| ขา | สถานะ |
|---|---|
| ลูกค้าสั่ง → เข้าระบบ | ✅ พร้อม (LIFF สั่งได้ · auto-promo · merge กันลูกค้าซ้ำ · สมัครสมาชิก no-OTP) |
| ตารางแมสให้เห็น | ✅ พร้อม (จัดเขต 8 ทิศจากพิกัด · เลือกวัน · รายการของ · เลือกผู้ส่งบันทึกได้ · กันงานนอกเขต) |
| **ข้อมูลของที่ต้องส่ง** | 🔴 **ไม่พร้อม — ดูบล็อกเกอร์ #1** |

---

## 🔴 บล็อกเกอร์ #1 — ออเดอร์ล่วงหน้า 67% ไม่มีรายการอาหาร

**16 จาก 24 ใบ** (query สด 5 ส.ค.) มีแต่บรรทัดควบคุม (`menu_code` = `1` แยกวันส่ง / `0` เพิ่มเติม) หรือ `menu_name` ว่าง
→ **แมส/ครัวไม่มีทางรู้ว่าต้องส่งอะไร**

```
5 ส.ค.  HT-1187334493 มีมี่ 🚚นอกเขต · HT-974958793 นวภัท · HT-990529586 พัดชา
        HT-1625063037 พรรณพิไล · HT-656899190 วรฉัตร · HT-669650018 คุณชัย
6 ส.ค.  HT-740336102 Chanya
7 ส.ค.  HT-720274618 Monchuta 🚚 · HT-128497743 นวภัท · HT-1243770593 มีมี่ 🚚
        HT-1901217400 วรฉัตร · HT-1811881447 พัดชา
11 ส.ค. HT-1394939703 Monchuta 🚚 · HT-681637796 นวภัท · HT-1933398303 พัดชา
12 ส.ค. HT-1830189274 ชลธิชา
```

**ระบบทำอะไรได้แล้ว:** หน้าแมสขึ้นแถบแดงเตือนชัด + แยกข้อความ *"ไม่มีรายการเลย"* vs *"มีบรรทัดแต่ใช้ไม่ได้"* · มี fallback อ่านจาก `mp_deliveries` (แต่ `mp_deliveries` ล่วงหน้า = **0 แถว** → fallback ไม่มีอะไรให้ดึง)

**ทางแก้ (U ทำเองไม่ได้):**
- P ดึงรายละเอียดจาก Hato portal (ต้อง token — bash เข้าไม่ได้)
- หรือ แอดมินกรอกมือ · **ห้ามเดาเมนูเด็ดขาด** (เรื่องแพ้อาหาร)

---

## ✅ เสร็จแล้วใน session 13 (อยู่บน main หมด)

| งาน | commit |
|---|---|
| Lalamove API ใช้จริง + เปิด slot "ส่งระบุเวลา" + แก้ราคาค้างตอนย้ายหมุด | `91b1d8b` `3a8f27f` `064cad9` |
| ซ่อมรูปเมนูแหว่ง 24 ตัว (ไฟล์ Storage ถูกตัดกลางคัน) + `scripts/check-menu-images.mjs` | `37f2c62` |
| OH ออเดอร์: แบ่งหน้าจริง + dropdown วันที่ + ช่วงวันที่ + เรียงตามวันส่ง | `9ce66c0` `ff0750e` `89db11b` |
| OH: กดแถวออเดอร์ = รายละเอียดเต็ม (สินค้า/ยอด/แต้ม/สมาชิก) | `3173d44` |
| แมส: รายการของที่ต้องขน + เตือนใบไม่มีของ + บันทึกผู้ส่ง + guard นอกเขต 60 กม. | `a115a5e` `47918dc` `76d7d47` |
| แมส: เตือน items ขยะ + fallback `mp_deliveries` (คีย์ปลอดภัย) | `9a460ef` |
| ข้อจำกัดอาหาร ครบวงจร (LIFF + OH) + ป้าย "มีผลกับ Meal Plan เท่านั้น" | `459c7a8` `0a4a15b` |
| DB: กระดิ่งแจ้งเตือน 🔔 (ไปทวงลูกค้า/ค้างโอน/ของใกล้หมด) | `d9d8ff7` |
| DB: ตัวเลข "ขายจริงบน LIFF" แทนเลขสูตรที่ทำให้เข้าใจผิด | `6642893` |
| KQ: โหมดแท็บเล็ตครัว + เป้าแตะมือถือ 44px | `5dd12ab` `fdd731a` |
| KQ: เตือนเมื่อกรอกผลิตได้จริงบนเมนู "ไม่จำกัด" (silent no-op) | `a8a3c44` |
| SQL รันเอง: `orders.courier` · `customer_preferences` | — |

---

## 📊 สถานะข้อมูลจริง (5 ส.ค. · query สด)
```
เมนูเปิดขาย            118      (ไม่มีรูป 2: MC2, S205)
ตั้งสต็อคจริง           22/118   (ที่เหลือ = ไม่จำกัด)
ลูกค้า                 8,019
ออเดอร์                15,910   (ล่วงหน้า 24 ใบ)
customer_preferences   0        ⚠️ ตารางมี แต่ยังไม่มีข้อมูลสักคน
mp_deliveries ล่วงหน้า  0        ⚠️ ว่าง
is_weekly_special      1 แถว    ⚠️ ไม่มีใคร maintain
subcode                0 แถว    ⚠️ ยังไม่มีใครกรอก
```

---

## 🪤 กับดักที่กัดซ้ำ — อ่านก่อนทำงาน

1. **ข้อมูลเก่าคือศัตรู** — ทุกห้องรายงานเลขผิดกันไปมาทั้งอาทิตย์ (ผมเองเคย verify 137 เมนู แต่จริง 118 · CC บอกเมนูไม่มีรูป 0 แต่จริง 2 · CC บอกงานที่เสร็จแล้วว่ายังค้าง 3 รอบ)
   → **verify กับ `origin/main` + Supabase สดเสมอ ห้ามอ่านจาก working tree ตัวเอง**

2. **🔥 ห้ามหยอดข้อมูลเทสด้วยชื่อลูกค้าจริง + วันจริง** — session 13 ทำพลาด: สร้าง `mp_deliveries` เทสชื่อ "มีมี่" ลงวัน 5 ส.ค. → CC ไปเจอนึกว่าของจริง → **เกือบให้ครัวทำอาหารตามเมนูเทส** · ถ้าต้องเทส ใช้ `__TEST__` นำหน้าชื่อ (เห็นบนหน้าจอ) ไม่ใช่ซ่อนใน `admin_notes`

3. **1000-cap** — `.select()` ไม่มี `.limit()` → Supabase คืนแค่ 1000 แถว เงียบๆ · โผล่ทุกหน้าที่อ่าน `orders` · **เหลือที่ยังไม่แก้: `report.html:138`**

4. **ไฟล์เป็น CRLF** — patch ด้วย script ต้อง normalize `\r\n`→`\n` ก่อน ไม่งั้น `includes()` ไม่เจอทั้งที่ข้อความตรง

5. **push แล้วต้อง verify ก่อนลบ worktree** — session 13 เสียงานไป 2 รอบเพราะ push ไม่ผ่าน (มีคน push แทรก) แล้วลบ worktree ทันที
   ```
   git push origin HEAD:main && git fetch origin main
   git show origin/main:<file> | grep -c "<marker ที่เพิ่งเพิ่ม>"   # ต้อง >0 ก่อนค่อยลบ
   ```

6. **จับคู่ข้อมูลห้ามใช้ "ชื่อ"** — ใช้ `order_id` → `line_uid` → `customer_id` เท่านั้น (ชื่อซ้ำกันได้ = ที่มาของกับดักข้อ 2)

---

## 🤝 กติกาหลาย session (นัทเคาะ 3 ส.ค.)
- **U (ห้องนี้) = เจ้าของ** `operation_hub` · `kitchen_queue` · `main_database_v2` · `home_editor` · `liff_*` · `customer.html` → **main เป็นของ U/P**
- **P** = สะพาน Hato → เรา (sync/ข้อมูล/เมนูฝั่ง Hato)
- **CC** = ตรวจ + ประสาน + `docs/` → งานโค้ดอยู่ branch **`amdoit`**
- เจอบั๊กบ้านคนอื่น = ส่งให้เจ้าของ ไม่แก้เอง
- **นัทมอบอำนาจ CC จัดคิวทุกห้อง** — U เดินได้เลยไม่ต้องรออนุมัติ · รายงานที่ห้อง `u` ในบอร์ด (`session_messages`)

## ⚙️ วิธี deploy (shared working tree — ห้ามสลับ branch)
```bash
WT=/c/Users/PP/AppData/Local/Temp/claude/wtX
git fetch origin main && git worktree add --detach "$WT" origin/main
# แก้ไฟล์ใน $WT → node scripts/check-html-js.js "$WT/<file>"
git -C "$WT" add <file> && git -C "$WT" commit -m "..."
git -C "$WT" push origin HEAD:main
# ⚠️ verify ก่อน แล้วค่อย: git worktree remove "$WT" --force && git worktree prune
```

---

## 🔵 รอนัทเคาะ (AI ตัดสินแทนไม่ได้)
1. **`subcode` vs `is_weekly_special` ทับซ้อนกัน** ← ผมว่าเร่งสุด
   ทั้งคู่ตอบคำถามเดียวกัน "เมนูไหนคือของสัปดาห์นี้" · ทั้งคู่ยังไม่มีข้อมูล (0 / 1 แถว) · ปล่อยไว้ = 2 แหล่งความจริงขัดกัน แล้วนิว/ครัว/LIFF อ่านคนละอัน
   **เสนอ:** `subcode` เป็นตัวหลัก (มีคนกรอกเป็นงานประจำสัปดาห์ + มีปุ่ม "ขึ้นสัปดาห์ใหม่") แล้ว `is_weekly_special` derive จากมันได้
2. **ใบงานครัวตามแพทเทิร์น R&D** — สเปคนิ่งแล้ว (`rnd/kitchen_0508.html`) รอสั่ง build
3. **split / เลื่อนวันเอง (self-service)** — สเปคนัทเคาะแล้ว: เลื่อนได้ล่วงหน้า **10 วัน** (เขียนบอกในหน้าเปลี่ยนวัน) · **split = คิดค่าส่งเพิ่ม** · **เลื่อนยกชุด = ไม่คิด** · **ไม่จำกัดจำนวนครั้ง** · ระบบต้อง auto สร้างรายการรับของใบใหม่
   → โครงรองรับแล้ว (1 รอบ = 1 order ตั้งแต่ u0.4.25) **ไม่ต้องรื้อสคีมา**
4. **Lalamove จองจริง (place order)** — U + CC เห็นตรงกันว่า **หลัง cutover** · ต้องมี webhook รับสถานะ + ปุ่มยืนยัน + กันกดซ้ำ · guard 60 กม. พร้อมแล้ว
5. **เรท Lalamove ดูถูกผิดปกติ** — API คืน 6.2 กม. ฿71 · 30.4 กม. ฿127 · 48.9 กม. ฿197 · **ยังไม่แตะเลขในโค้ด** รอนัทเทียบกับแอปจริง
6. **2 เมนูไม่มีรูป** MC2 · S205

## 👤 รอนัทกดเอง
- เทส LIFF บนมือถือจริง (สั่ง 1 ใบ · สมัคร · ย้ายหมุดดูเรทขยับ · สั่งด้วยเบอร์ลูกค้าเก่าดู merge)
- wire ริชเมนู LINE "สมัครสมาชิก" → `liff_register.html`
- ลองโหมดแท็บเล็ตบนแท็บเล็ตจริงตอนซื้อมา

## 🟡 ค้างทางเทคนิค (หลัง beta ได้)
- `report.html:138` 1000-cap
- แก้รายกล่องใน Meal Plan (ตอนนี้เก็บได้แค่ระดับคน — ใบงานครัวมี "อุษณี ข้าวไรซ์เบอร์รี่ 1 กล่อง" แบบนี้ระบบยังทำไม่ได้)
- Hyrox 30 กล่อง + Bone Broth ยังไม่อยู่ในระบบ MP
