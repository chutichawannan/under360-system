# 🔍 Audit ก่อน Cutover Hato (3 ส.ค. 2026 · นัทสั่ง)

> เป้า: หาบั๊กแฝงแบบ "เงียบ" (ไม่มี error ไม่มีสัญญาณ) ก่อนถอด Hato ที่เป็นตาข่าย
> วิธี: sweep โค้ดทุกไฟล์ที่อ่านตารางใหญ่ + verify จำนวนแถวจริงจาก DB (ไม่เดา)

**ตัวเลขจริงตอน audit:** orders 15,858 · customers 8,018 · order_items 41,058 · menu_items 423 (ตั้งสต็อกจำกัด 22 ตัว)

---

## 🔴 ต้องแก้ก่อน cutover

### 1. ไม่มีการตัดสต็อกอัตโนมัติเลย (oversell)
- `liff_customer.html` อ่าน `stock_total` (`effectiveStock` บรรทัด ~1756) **แต่ไม่พบโค้ดใดๆ ที่ลด `stock_total` ตอนลูกค้าสั่ง** (grep ทั้งไฟล์: เจอแค่ comment + อ่านค่า)
- จุดที่เขียน `stock_total` มีแค่ **แอดมินปรับมือ** (`kitchen_queue.html` แท็บตั้งสต็อค · `main_database_v2.html`)
- **ผล:** ลูกค้าสั่งไปเท่าไหร่ตัวเลขไม่ลด → badge "เหลือ N ชิ้นสุดท้าย" ไม่จริง → **ขายเกินได้** (กระทบ 22 เมนูที่ตั้งสต็อกจำกัด · เมนู `NULL` = ไม่จำกัด ไม่กระทบ)
- กันชนที่มีตอนนี้ = เช็ค remaining **ในตะกร้าของ browser ตัวเองเท่านั้น** (ข้ามคนไม่รู้กัน)
- ⚠️ ต้องเคาะก่อนแก้: ตั้งใจให้แอดมินคุมมือ หรือควรตัดอัตโนมัติ? (ถ้าตัดอัตโนมัติ ควรทำที่ DB ระดับ atomic กันสั่งพร้อมกัน)

### 2. `customer.html` — บั๊กซ้อน 3 ชั้น (แบบเดียวกับที่ OH เพิ่งแก้ใน u0.4.41 แต่ไฟล์นี้ยังไม่ได้แก้)
- บรรทัด 141: `customers.select("*")` **ไม่มี limit/range** → ได้แค่ **1,000 จาก 8,018** (เรียง created_at asc = ได้ลูกค้าเก่าสุด · **ลูกค้าใหม่หายหมด**)
- บรรทัด 148: `orders.select(...)` ไม่มี limit → 1,000 จาก 15,858
- บรรทัด 148/219: ใช้คอลัมน์ **ที่ไม่มีจริง** `customer_line_uid` / `total_price` (ของจริง = `line_uid` / `total`) → ยอดสะสม/ประวัติผิด

### 3. `liff_profile.html` — หน้า "สั่งซ้ำ" โชว์เมนูปลอม
- บรรทัด 242 select `menu_items(... stock_quantity)` — **`stock_quantity` ไม่มีอยู่จริง** (ของจริง = `stock_total`) → query error → ตกไป **mock data** (บรรทัด 246: "อกไก่ย่างสมุนไพร/สันในหมูย่าง" hardcode)
- ลูกค้าเห็นรายการเมนูที่ไม่ใช่ของที่ตัวเองสั่ง

---

## 🟡 ปานกลาง (ตัดข้อมูลเงียบ แต่ยังไม่พังตอนนี้)
| ไฟล์ | จุด | อาการ |
|---|---|---|
| `operation_hub.html:850` | `orders.select("*")` ไม่มี limit เมื่อไม่ได้กรองวัน | เห็นแค่ 1,000 ใบล่าสุด (desc = ใหม่สุด ยังพอใช้ แต่ตัดเงียบ) |
| `report.html:138` | เหมือนกัน + nested `order_items` | รายงานคำนวณจากข้อมูลไม่ครบ |
| `operation_hub.html:1801` | `mp_deliveries.select("*")` ไม่มี limit | ตอนนี้ 27 แถว = ปลอดภัย · จะโตขึ้น |

## ✅ ผ่าน (ตรวจแล้วปลอดภัย)
- `kitchen_queue.html` — filter `delivery_date >= today-3` → **ปัจจุบัน 16 ใบ** ✓ + chunk `order_items` ทีละ 80 ✓
- `pwa/orders_upcoming.html` — date range + `limit(2000)` ✓
- `command_center.html` — `limit(3000)` (60 วัน ~600 ใบ) ✓
- `kitchen_display.html` — `eq(delivery_date)` ✓ · `api/notify-daily-orders.js` — `limit(4000)` ✓

> ⚠️ **หมายเหตุสเกล:** KQ/PWA ปลอดภัยเพราะ**กรองวันที่** ไม่ใช่เพราะ limit — ถ้าวันไหนออเดอร์ในหน้าต่างเกิน 1,000 ใบจะโดนตัดเงียบอีก (ยังห่างมาก ปัจจุบัน 16)

---

## 📌 สิ่งที่ audit นี้ **ยังไม่ได้ตรวจ** (พูดตรงๆ ไม่เหมาว่าครบ)
- flow end-to-end จริงบนมือถือ (สั่ง→ครัวเห็น→ส่ง→จ่าย) — ต้องกดจริง
- RLS/สิทธิ์ anon แต่ละตาราง · การแข่งกันเขียน (2 แอดมินแก้ออเดอร์เดียวกัน)
- quota Google Maps/Supabase ตอน traffic เยอะ · ไม่มีระบบแจ้ง error ให้เรารู้เมื่อลูกค้าเจอปัญหา
