# 🫡 HANDOFF — Command Center (CC) · 5 ส.ค. 2026

> เขียนตอนนัทสั่งแยก session ใหม่: **"command center"** (จ่ายงาน/ประสาน) กับ **"cc etc"** (คุยเรื่องงานทั่วไป)
> session เดิมยาวมากแล้ว — ไฟล์นี้คือทุกอย่างที่ต้องรู้เพื่อรับช่วงต่อ

---

## 1. บทบาท CC (นัทมอบไว้)
- **เลขากลางทั้งโปรเจค** — รวบสถานะทุกห้อง · จ่ายงาน · สรุปให้นัทรอบเดียว
- **นัทมอบอำนาจเต็มแล้ว (5 ส.ค.):** *"คอขวดมาก · ให้ CC จัดการทั้งหมด ไม่ต้องรอฉัน · เดี๋ยวฉันตรวจงานเอง"*
- **⚠️ กติกาที่นัทตั้ง:** ถาม-ตอบไป-กลับกับ session อื่นได้ **3-5 รอบต่อครั้ง ไม่เกินนี้** → แล้วกลับมา catchup ให้นัท
- **งานโค้ดของ CC อยู่ branch `amdoit`** (นัทตั้งชื่อเอง = "แอม ดู อิท") — **main เป็นของ U/P** ห้ามชนกัน

## 2. กติกาแบ่งงาน 3 ทีม (นัทเคาะ 3 ส.ค.)
| ทีม | ถือไฟล์ |
|---|---|
| **U** (13 U-track) | `operation_hub` · `kitchen_queue` · `main_database_v2` · `home_editor` · `liff_*` · `customer.html` |
| **P** (11 P-track) | สคริปต์ sync + ข้อมูลฝั่ง Hato (จนถึง 7 ส.ค.) |
| **CC** | `command_center` · `docs/` · `scripts/backup*` · งานไม่มีเจ้าของ |

**กฎกันชน:** เจอบั๊กบ้านคนอื่น = ส่งให้เจ้าของ · เช็คก่อนว่าเขาแก้ไปหรือยัง · **pull ก่อนแก้ + deploy ผ่าน worktree จาก origin** (ห้าม `cp` ไฟล์ทับ — เคยย้อนงานคนอื่นหายเงียบ)

## 3. ⏰ Deadline ที่ยังอยู่
**Hato ถูกบอกเลิก 7 ส.ค. 2026** (นัทยืนยันไม่ต่อ · เสียเงินมหาศาลถ้าต่อ)
- ✅ เว็บ/บล็อก **ไม่มีลิงก์ Hato สักจุด** (M ยืนยันด้วย grep + query DB) → เว็บไม่กระทบ
- 🔴 ที่ยังเสี่ยง: ข้อมูลที่ต้องดึงจาก Hato ก่อนตาย (ดูข้อ 5)

## 4. 🔴 เรื่องด่วนที่ยังไม่จบ (ส่งต่อ)
**ออเดอร์ที่ระบบไม่รู้ว่าต้องขนอะไร** — ใบ ฿0 จาก Hato มี `order_items` อยู่จริงแต่ข้างในเป็นขยะ (`menu_code = "1"` / `"0"`)
- `HT-1187334493` มีมี่ (5 ส.ค.) · `HT-974958793` นวภัท (5 ส.ค.) · อีก 3 ใบ 7 ส.ค. · 2 ใบ 11 ส.ค.
- ⚠️ **ห้ามเดาเมนูเด็ดขาด** (เรื่องแพ้อาหาร) → P ต้องดึงจาก Hato หรือแอดมินกรอกมือ
- 🎓 **บทเรียนสำคัญ (4-5 ส.ค.):** CC เคยเจอเมนูของมีมี่ใน `mp_deliveries` แล้วนึกว่าเป็นของจริง — **ที่จริงเป็นข้อมูลเทสของ U ที่ใช้ชื่อลูกค้าจริง + วันจริง** (U จับได้เองและลบแล้ว) → **ห้ามจับคู่ข้อมูลด้วย "ชื่อ + วันที่" เด็ดขาด ใช้ `order_id` → `line_uid` → `customer_id` เท่านั้น · จับคู่ไม่ได้ต้องขึ้นแดง ไม่ใช่เดา**

## 5. สถานะแต่ละห้อง (ณ 5 ส.ค.)
| ห้อง | สถานะ |
|---|---|
| **13 U** | 🟢 เร็วมาก — กระดิ่งแจ้งเตือนเสร็จ (CC ตรวจแล้วผ่าน) · driver-select/customer_preferences/16 เมนู เสร็จหมด · เหลือ fallback เมนู MP + ใบงานครัว |
| **11 P** | 🟡 ทำงานอยู่ — ค้าง: เมนูของออเดอร์ ฿0 · **รูปการ์ดหน้าโฮมที่ยัง hotlink Hato** (ไม่ย้าย = 8 ส.ค. หน้าโฮมโล่ง) · รูปเมนู 335 · รายงาน Hato |
| **02 M** | 🟢 พร้อม — เหลือ **นัทแก้ DNS 4 record ที่ Wix** (M อ่านหน้า Wix ไม่ออก) · redirect verify ผ่านแล้ว |
| **12 นิว** | 🔓 เพิ่งปลดล็อก (order_items + preferences + subcode + สต็อก พร้อมหมด) — CC สั่งให้เริ่ม v1 แล้ว |
| **05/06 เอิธ** | 🟡 CC สั่งเซ็ต routine เช็ค feedback ลูกค้า · wave 2 รอนัท login LINE |
| **10 เตียง** | 🟡 CC ปลดล็อกเรื่องรูป + เตือนอย่าใช้ลิงก์ Hato |
| **08 F** | ⏸️ รอ PDF statement จากนัท (ไม่มีอะไรทำต่อได้) |
| **R&D 01** | 🟢 ทำงานกับนัทตรง — ส่งแพทเทิร์นใบงานครัวให้ U แล้ว |

**แนะนำให้ปิด/รวม:** Hyrox mealplan brief (ซ้ำ R&D 01) · สินค้าขาดรูปภาพ (ซ้ำงาน P) · จัดระเบียบโฟลเดอร์ (เสร็จแล้ว) · Agent-Earth (แทนด้วย 05/06) · กลับประเทศไทย (จบแล้ว) · 09 Widget (เสร็จ a0.6)

## 6. ของที่ CC ทำเสร็จแล้ว (อย่าทำซ้ำ)
- **audit ทั้งระบบ 24 จุด + แก้หมด** → `docs/AUDIT_PRE_CUTOVER.md`
- **ระบบเงินครบวงจร** — สลิปเก็บรูปจริง + `pending_review` รอแอดมินตรวจ · ค่าส่งไม่ปักหมุดติดธง · เลขออเดอร์กันซ้ำ (unique index + retry)
- **วงจรสต็อกครัว** — ลูกค้าสั่ง = ตัดสต็อกอัตโนมัติ (RPC `adjust_stock`) · ครัวกรอก "ผลิตได้จริง" = ตั้งเลขขายทันที
- **ชื่อเล่นเมนู (subcode)** — `menu_items.subcode` (s1/xs3 · x = ค้างกี่สัปดาห์) · ช่องกรอกในหน้า DB เฉพาะสินค้าออนไลน์ · LIFF โชว์ให้ลูกค้า · ปุ่ม "🔄 ขึ้นสัปดาห์ใหม่" เติม x อัตโนมัติ
- **บอทผู้ช่วยครัวในกลุ่ม LINE** — `api/line-webhook.js` (เก็บแชททั้งหมด + ตอบยอดสั่งเมื่อถูกถาม) · **รอนัทรัน SQL + ใส่ token**
- **backup ข้อมูล** — `scripts/backup_data.sh` + scheduled task ทุกจันทร์ (ครั้งแรก 65,417 แถว)
- **case study** → `docs/CASE_01_wrong_box_count.md`
- **SQL ที่ CC รันเองผ่าน Supabase SQL Editor:** `slip_url` + bucket `payment-slips` + policy · unique index `orders.order_number` · function `adjust_stock` · คอลัมน์ `menu_items.subcode`

## 7. ⏳ คิวของนัท (อัปเดตล่าสุด — ดู `TODO.md` หัวข้อ 🔴)
1. **เทสสั่งจริง 1 ใบบนมือถือ** ← ปิดท้าย beta
2. **ย้ายริชเมนู LINE OA → LIFF 360** (หลังข้อ 1)
3. **แก้ DNS 4 record ที่ Wix** (~5 นาที · ไม่เร่ง เพราะเว็บไม่พึ่ง Hato)
4. รัน `scripts/sql_line_group_log.sql` + ใส่ LINE token → เปิดบอทกลุ่มครัว
5. ทำ repo เป็น private (CC ลองแล้วปุ่มไม่ทำงาน + จอค้าง)
6. สั่งครัวกรอก "ผลิตได้จริง" ใน KQ รอบเดียว (22 เมนูตั้งขายไว้แต่ครัวนับได้ 0)

## 8. 🎓 บทเรียนที่ CC เก็บได้ (ห้ามพลาดซ้ำ)
1. **ห้าม `cp` ไฟล์จาก working tree ทับตอน deploy** — เคยทำ favicon หายทั้งไฟล์แบบเงียบ → แก้ใน worktree ที่ base จาก origin เท่านั้น
2. **pipe บัง exit code** — `cmd | tail` คืน exit ของ `tail` → เคย push ไฟล์ครัวที่ syntax พังขึ้น main · ต้องเก็บ exit code ก่อน pipe
3. **ข้อมูลเก่าคือศัตรู** — CC เคยรายงานงานที่ U ทำเสร็จแล้วว่า "ยังค้าง" 3 รายการ + บอกว่า Lalamove ยังไม่มี key ทั้งที่ใช้งานได้จริง → **verify กับ DB/โค้ดจริงก่อนพูดทุกครั้ง**
4. **ข้อมูลเทสที่ใช้ชื่อจริง = ระเบิดเวลา** (ดูข้อ 4)
5. **ตรวจก่อนรายงานบั๊ก** — เกือบรายงานบั๊กผิดให้ U เพราะ innerText ดึงมาแล้วดูติดกัน ที่จริงเป็นชิปแยกสวยงาม

---

# 📌 ภาคผนวก — เก็บตกรอบท้าย (5 ส.ค. เย็น)

## A. งานใหม่ที่นัทสั่งรอบสุดท้าย → มอบ **U + พี่เก่ง**
1. **ปุ่มเดียวเรียก Lalamove จากหน้าแมส/ครัว** — ต่อยอด `api/lalamove-quote.js` (ใช้ได้จริงแล้ว) ให้ **place order** ไม่ใช่แค่ขอราคา · ช่วงแรกให้แอดมินยืนยันก่อนจองจริงเสมอ · นัทอยากให้ **CC สั่งจองจากแชทได้** ในเฟส 2
2. **เครื่องคำนวณจัดรอบส่ง** — เทียบ แยกคันต่อจุด vs คันเดียวหลายจุด vs แบ่งตามช่วงเวลา → บอกว่าถูกสุดแบบไหน · **ติดที่ API รองรับจุดเดียว ต้องเพิ่ม multi-stop ก่อน**

## B. ผลตรวจหน้าแมส 5 ส.ค. (CC ทำจริงแล้ว)
| ออเดอร์ | ปลายทาง | ผล Lalamove จริง |
|---|---|---|
| นวภัท (คลองเตย 11.5 กม. · 14:00-15:00) | กทม. | **฿104** มอเตอร์ไซค์ ✅ |
| มีมี่ (ร้อยเอ็ด **502 กม.**) | ตจว. | ❌ **ปฏิเสธ `ERR_OUT_OF_SERVICE_AREA`** |
| U-0805-001 | — | ใบเทส (cancelled) |

→ **สรุป: 5 ส.ค. มีงานแมสจริงแค่ 1 จุด** → ยังคำนวณการจัดรอบไม่ได้ (ต้องมี ≥3 จุด)
→ 🔴 **บั๊กที่เจอ:** ใบร้อยเอ็ดติดป้าย `delivery_type = messenger` ทั้งที่เป็นงานขนส่ง/ฟรีซแพ็ค → **ต้องมีกฎกันก่อนเปิดปุ่มจอง** (เกิน ~60 กม. หรือนอกเขตบริการ = ไม่ใช่งานแมส)

## C. เก็บตกที่ทำไปแล้วรอบท้าย
- ✅ **commit `.claude/agents/nong-tiang.md` + `web/tiang/`** — ไม่เคยถูก commit เลย จึงหายตอนย้ายเครื่อง `C:\Users\360 User1\` → `C:\Users\PP\` (`nong-eath.md` tracked อยู่แล้ว)
- ✅ **ปิด Known Issue #12** — ราคา `mp_offer_sets` ตรงราคาโฆษณาแล้วทุกตัว (CC verify กับ DB เอง) · มีเซ็ต `hyrox` ฿4,500 เพิ่มในตาราง
- ✅ **ยกเรื่องรูป Meal Plan เป็นงานมีเจ้าของ** — เตียงชี้ต้นทุนจริง: สัปดาห์ 6–12 ส.ค. **ไม่มีโพสต์ MP เลยสักใบ** เพราะ Storage ไม่มีรูป MP (0 รูป) → ตัวชูโรงหายทุกสัปดาห์จนกว่าจะได้รูป
- ✅ เตียงส่งชีท 6–12 ส.ค. **7/7 paste-ready** (ทุกรูป verify HTTP 200)

## D. ⚠️ ตัวเลขที่ถูกแก้รอบท้าย (อย่าใช้ของเก่า)
- **เมนูเปิดขาย = 118** (ไม่ใช่ 137 — CC เคยรายงานผิด · เตียงจับได้ · verify แล้ว)
- **`menu_items.subcode` = ว่างทุกแถว 0 ตัว** — ระบบพร้อมแต่**ยังไม่มีใครกรอก** · อย่าบอกห้องอื่นว่าใช้ได้เลย
- **`is_weekly_special` = 1 แถว** (S136) — flag ยังไม่ maintain แยกเมนูพิเศษ 8+5 ไม่ได้

## E. 🎓 บทเรียนเพิ่ม (ต่อจากข้อ 8 ด้านบน)
6. **สร้างเครื่องมือ ≠ มีข้อมูล** — CC สร้างช่องกรอก subcode แล้วบอกห้องอื่นว่า "ใช้ได้เลย" ทั้งที่ตารางว่างเปล่า → **ก่อนบอกใครว่าใช้ได้ ต้อง query ดูว่ามีข้อมูลจริงไหม**
7. **ไฟล์ที่ไม่ commit = ของที่จะหาย** — agent ของเตียงหายตอนย้ายเครื่อง · ของสำคัญต้องเข้า git ทันทีที่สร้าง
8. **ป้ายประเภทงานผิด = อันตรายตอน automate** — ใบร้อยเอ็ดติดป้าย messenger ไม่เป็นไรตอนคนอ่านเอง แต่**พอมีปุ่มจองอัตโนมัติจะกลายเป็นปัญหาทันที** → ทำ automation เมื่อไหร่ ต้องตรวจ data quality ก่อนเสมอ
