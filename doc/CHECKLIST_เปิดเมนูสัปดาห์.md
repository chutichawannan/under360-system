# ✅ เช็คลิสต์เปิดเมนูสัปดาห์ใหม่ (S1–S8 / D1–D5)

> **นัทสั่งทำ 19 ก.ย. 2026 หลังเปิดขายแล้วลูกค้าเห็นแค่ 1 ใน 13 เมนู**
> *"ไว้ใจไม่ได้เลย ทำไมไม่ทำเช็คลิสสสส"* · *"ไปทำเช็คลิสด้วยว่าให้เช็คย้อนว่าขึ้นหรือยัง"*
> **ห้องนิวต้องไล่ทีละข้อทุกสัปดาห์ ห้ามข้าม ห้ามจำเอา**

## 🔴 สิ่งที่พลาดมาแล้วจริง (อ่านก่อน)
| วันที่ | พลาดอะไร | ทำไมไม่มีใครจับได้ |
|---|---|---|
| 19 ก.ย. 69 | เปิดขายครบ 13 ตัวใน DB แต่ **12 ตัวอยู่หมวด `hato_import` ที่ลูกค้าไม่เห็น** → หน้าลูกค้ามีแค่ S5 | ตรวจแต่ `is_available` ไม่เคยตรวจ **หมวด** · ไม่ได้เปิดหน้าลูกค้าดูย้อนหลังการเปิด |
| 19 ก.ย. 69 | ถอดชื่อเล่นชุดเก่าออกแล้วชุดใหม่ไม่โผล่ = **ทั้งหน้าไม่มี S1-S8 เลย** | ไม่ได้เช็ค "หลังเปิดแล้วหน้าลูกค้ามีครบ 13 ป้ายไหม" |

---

## ① ก่อนเปิด — ของต้องครบ
- [ ] `weekly_subcode_plan[<จันทร์>]` ครบ 13 ช่อง · `weekly_stock_plan[<จันทร์>].qty` ครบ 13 ช่อง
- [ ] เมนูทั้ง 13: `available_from = <จันทร์>` · `subcode` ตรงช่อง · โภชนาการครบ 4 ค่า · มีรูป · ราคา S≥125 / D≥80
- [ ] **ไม่ซ้ำ 3 เดือน** — เทียบทั้ง `doc/weekly_menu_plan/LAST_USED.md` **และ** ออเดอร์จริง
- [ ] ด่าน 05 `pass=true` **และ** `visual_ok=true` (ถ้าเปลี่ยนรูปทีหลัง = ลายนิ้วมือเปลี่ยน ต้องให้ 05 กดใหม่)
- [ ] 🔴 **หมวดต้องถูก: S → `no_special` · D → `pack_regular`** (หมวดอื่น เช่น `hato_import` = ลูกค้าไม่เห็น)
- [ ] สำรองข้อมูลก่อนเขียนลง `Desktop/under360_backups/<วันที่>_open<สัปดาห์>/`

## ② ตอนเปิด
- [ ] ถอดชื่อเล่น S1-S8/D1-D5 ของชุดเก่าออก (ไม่ปิดการขายชุดเก่า — ของยังขายต่อได้)
- [ ] เปิดขาย 13 ตัว + ตั้งสต็อก (`stock_total = นับได้ + ยอดผลิต`) + ลงช่อง "กำลังผลิต" ให้ครัว

## ③ 🔴 เช็คย้อนหลังเปิด — ข้อที่เคยข้ามแล้วพัง
- [ ] `node scripts/niw/check_week_live.mjs <จันทร์>` → **ต้องขึ้น Y ครบทุกคอลัมน์ทั้ง 13 แถว** (เปิด · หมวดถูก · ธง · รูป · แคล) และ "ชุดสัปดาห์ก่อนที่ยังมีชื่อเล่นค้าง = ไม่มี"
- [ ] **เปิดหน้าลูกค้าจริงดูด้วยตา** — นับให้ครบ **S1–S8 ในแท็บข้าวกล่อง · D1–D5 ในแท็บแพคกับข้าว** · ป้ายสัปดาห์ต้องเป็นสัปดาห์ใหม่
      ⚠️ ห้องนิวเปิด LIFF เองไม่ได้ (ต้องล็อกอิน LINE) → **ฝาก 05 หรือคนที่ล็อกอินได้เปิดดู แล้วส่งภาพหน้าจอกลับมา** ยังไม่เห็นภาพ = ยังไม่จบงาน
- [ ] บอกผลเป็นตัวเลขทุกครั้ง: เปิดกี่ตัว / ถอดป้ายเก่ากี่ตัว / ป้ายซ้ำกี่อัน / หน้าลูกค้าเห็นกี่ตัว

## ④ ปิดงาน
- [ ] อัปเดต `doc/weekly_menu_plan/` (`node scripts/niw/build_weekly_plan.mjs`)
- [ ] ส่งโบรชัวร์ให้ 05 ไว้ทำบรอดแคสต์
