-- ⚠️ ไฟล์นี้ไม่ต้องรันแล้ว (6 ส.ค. 2569)
--
-- เดิมจะสร้างตาราง delivery_cost_log ไว้เก็บสถิติค่าส่งรายวัน
-- แต่ SQL ค้างรอคนรันอยู่ 2 วัน (Claude รัน SQL เองไม่ได้ — Supabase dashboard
-- ไม่ render ในเบราว์เซอร์ที่ใช้อยู่ · ลองแล้ว 4 วิธีไม่ผ่าน)
--
-- → เปลี่ยนไปเก็บใน `kitchen_data` แทน (ตารางที่มีอยู่แล้ว · anon เขียนได้ทันที)
--   คีย์: delivery_cost_YYYY-MM-DD   ·   ค่า: jsonb เหมือนเดิมทุก field
--   โค้ดอยู่ที่ operation_hub.html → msgLogCost() / msgShowCostHistory()
--
-- ดูสถิติที่เก็บไว้:
--   select key, data->>'stops' as จุดส่ง, data->>'cost_grouped' as ค่าส่ง
--   from kitchen_data where key like 'delivery_cost_%' order by key desc;
--
-- ถ้าวันหลังข้อมูลเยอะจนอยากได้ตารางจริง ค่อยย้ายทีเดียว (ข้อมูลอยู่ใน jsonb ครบแล้ว)
