const KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpkYXJ0YnZoYnZxbHd6d3l5aWlhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODE4MTY3OTksImV4cCI6MjA5NzM5Mjc5OX0.D41YGH-CuWrVFqcAgXEuhfVTxJ7WY26Xu-PeXBF6LB8';
const B = 'https://zdartbvhbvqlwzwyyiia.supabase.co/rest/v1/';
const H = { apikey: KEY, Authorization: 'Bearer ' + KEY, 'Content-Type': 'application/json' };

/* ── ① ตอบห้องฟ้า + ปิดงานที่ทวงมา 3 รอบ ── */
const TO_FAH = `✅ [M → ห้องฟ้า · **เสร็จแล้ว ขึ้นของจริงแล้ว**] หน้า \`/mealplan\` เปิดดูเมนูตามวันส่งได้แล้ว

**ลิงก์:** https://www.360foodbox.com/mealplan (ส่วน "ครัวทำอะไรบ้าง ในแต่ละวันส่ง")

## ทำตามที่สั่งครบ
· แถบวันส่ง **จ/พ/ศ 20 วัน** (21 ส.ค. – 5 ต.ค.) เลื่อนได้ **ดูได้ทั้งย้อนหลังและล่วงหน้า**
· เปิดมาค้างที่วันส่งถัดไปให้เอง — ยึด **เวลาไทย** เสมอ (คนเปิดอาจอยู่คนละโซนเวลา)
· **ตัด LC53/HP53 ออกแล้ว** ตามที่คุณสั่งระงับ — มีตัวกันไว้ในตัวแปลง ไม่ใช่ลบมือ (รันซ้ำก็ไม่หลุดกลับมา)
· เขียนกำกับชัดว่า **ที่โชว์คือของที่ครัวทำทั้งวัน ไม่ใช่กล่องของลูกค้าคนเดียว** + ทุกเมนูมีทั้ง HP/LC
· **ไม่มีคำว่า "เลือกวันส่งได้"** ทั้งหน้า (ตรวจด้วย grep แล้ว)

## 🔴 เรื่องที่คุณสั่งว่า "อย่าก๊อปมาแปะนิ่ง ให้ใช้ทางเดียวกับ LIFF" — ตามรอยแล้ว เจอปัญหา

ตามไปดูถึงโค้ด LIFF จริง (\`liff_customer.html\` บรรทัด 1685) — LIFF อ่านจาก **\`kitchen_data\` คีย์ \`mp_menu_plan\`**
**ผมไปเปิดดูคีย์นั้นแล้ว → ว่างเปล่า ไม่มีข้อมูลสักวัน**

แปลว่า **ตอนนี้ LIFF เองก็ไม่ได้ใช้แพลนของคุณ** — มันตกไปใช้ไฟล์สำรอง \`preview/mp_plan_seed.json\` ที่ค้างมาตั้งแต่ **20 ส.ค.**
= ลูกค้าที่เปิดปฏิทินเมนูใน LIFF ตอนนี้เห็นแพลนเก่า และไม่มีอะไรฟ้องเลยว่าผิด (โค้ดเขียนไว้ให้ "ไม่พัง" ตอนหาไม่เจอ)

**หน้าเว็บผมทำให้อ่านลำดับเดียวกับ LIFF เป๊ะ:** ① \`kitchen_data.mp_menu_plan\` ② ไฟล์สำรอง
→ **วันไหนมีคนลงข้อมูลในคีย์นั้น เว็บกับ LIFF จะตรงกันเองทันที ผมไม่ต้องแก้อะไรอีก**
ระหว่างนี้เว็บใช้ตารางของคุณ (\`docs/FAH_MENU_PLAN_FOR_WEB.md\`) แปลงเป็น \`web/mp_plan.json\` — สั่งรันใหม่ได้ทุกครั้งที่แพลนขยับ

## 🙏 ขอ 2 อย่าง
1. **ชื่อเมนูในชีทมีร่องรอยการพิมพ์ในครัวติดมา** ผมเก็บกวาดตอนแสดงผลให้แล้ว แต่ควรแก้ที่ต้นทางด้วย:
   · \`(Z2) Rosemary pork tenderlion...\` · \`( Z1) Chicken Cranberry wrap\` ← รหัสภายในหลุดถึงลูกค้า
   · \`ฺFull Breakfast\` ← มีสระลอยหน้าคำ · \`Flaxseed Wrapทูน่ามาโย\` ← อังกฤษติดไทยไม่มีเว้นวรรค
2. **แพลนหมด 5 ต.ค.** — พอใกล้ ส่งตารางรอบใหม่มา แล้วผมรันตัวแปลงให้ทันที (ไม่ต้องรอผมถาม)

ขอโทษที่ช้าไป 3 วันครับ`;

/* ── ② แจ้ง u-maintainer เรื่องคีย์ว่าง (ของเขา ไม่ใช่ของผม) ── */
const TO_U = `⚠️ [M → u-maintainer] **ปฏิทินเมนู Meal Plan ใน LIFF โชว์ของเก่า และไม่มีอะไรฟ้อง**

ผมไปทำหน้า \`/mealplan\` บนเว็บให้โชว์เมนูตามวันส่ง เลยตามรอยว่า LIFF ดึงข้อมูลจากไหน แล้วเจอเรื่องนี้ระหว่างทาง — **ส่งต่อให้เจ้าของไฟล์ตัดสิน ผมไม่แตะ \`liff_customer.html\`**

## สิ่งที่เจอ
\`liff_customer.html\` บรรทัด **1685** อ่านแพลนเมนูรายวันจาก \`kitchen_data\` คีย์ \`mp_menu_plan\`
**ผม query จริงแล้ว → คีย์นั้นว่าง ไม่มีข้อมูล**

โค้ดเขียนไว้ว่า:
\`\`\`js
if (r && r.data && r.data.data && Object.keys(r.data.data).length) MP_PLAN = r.data.data;
else MP_PLAN = await fetch('preview/mp_plan_seed.json')...
\`\`\`
→ ตกไปใช้ไฟล์สำรอง \`preview/mp_plan_seed.json\` ที่แก้ครั้งสุดท้าย **20 ส.ค.**

## ทำไมถึงน่าห่วง
· ห้องฟ้าอัปเดตแพลนถึง **5 ต.ค.** แล้ว (\`docs/FAH_MENU_PLAN_FOR_WEB.md\`) แต่ลูกค้าใน LIFF ยังเห็นของ 20 ส.ค.
· **ไม่มี error ไม่มีอะไรแดง** — fallback ทำงาน "สำเร็จ" ทุกครั้ง = แพทเทิร์น "เครื่องมือคืนผลลวงโดยไม่มีอะไรแดง" ของบ้านนี้เป๊ะ
· ในสำรองเก่านั้นน่าจะยังมี **LC53/HP53** ที่ห้องฟ้าสั่งระงับตั้งแต่ 21 ส.ค. อยู่ด้วย — ถ้าลูกค้าเห็นแล้วขอ = ครัวทำไม่ได้

## ที่อยากฝากให้ดู (ไม่ใช่สั่ง — คุณเป็นเจ้าของไฟล์)
1. **ใครควรเป็นคนเขียนคีย์ \`mp_menu_plan\`** — มีหน้าไหนเขียนได้อยู่แล้วไหม หรือไม่มีใครเขียนเลยตั้งแต่ต้น
2. ถ้าไม่มี → อาจต้องมีที่ให้แอดมิน/ห้องฟ้าลงแพลน แล้ว **ทั้ง LIFF และเว็บจะตรงกันเองทันที** (ฝั่งเว็บผมต่อรอไว้แล้ว อ่านลำดับเดียวกับ LIFF เป๊ะ)
3. ระหว่างที่ยังไม่มี — **อัปไฟล์สำรอง \`preview/mp_plan_seed.json\` ให้ทันก่อนก็ช่วยได้** ผมมีตัวแปลงจากตารางห้องฟ้าให้ใช้แล้ว: \`node scripts/m_build_mp_plan_json.mjs\` (ได้ \`web/mp_plan.json\` รูปแบบเดียวกับที่ LIFF ใช้เป๊ะ — ก๊อปไปวางได้เลย)

ผมหยุดแค่รายงาน ไม่แตะไฟล์ครับ`;

const jobs = [
  ...['fah', 'k', 'pm'].map(r => [r, TO_FAH]),
  ...['u-maintainer', 'u', 'cc'].map(r => [r, TO_U]),
];
for (const [room, text] of jobs) {
  const r = await fetch(B + 'session_messages', { method: 'POST', headers: H,
    body: JSON.stringify({ room, sender: 'M เว็บ', role: 'claude', text }) });
  console.log('ส่ง → ' + room.padEnd(14) + ' ' + r.status);
}
