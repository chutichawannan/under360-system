/* หน้าแมสเห็นโน้ตลูกค้า — กิ๊ฟขอผ่านกะปัน 18 ก.ย. 2569 · ตัวอย่างทุกบรรทัดคือโน้ตจริงจาก DB */
import fs from 'node:fs';
const NL = String.fromCharCode(10);
const H = fs.readFileSync(new URL('../../operation_hub.html', import.meta.url), 'utf8').split(String.fromCharCode(13)).join('');
const a = H.indexOf('function msgNote(o){'), b = H.indexOf(NL + 'function msgNoteHtml(', a);
const msgNote = new Function(H.slice(a, b) + '; return msgNote;')();
let ok = 0, fail = 0;
const t = (n, got, want) => { if (got === want) { ok++; console.log('  ✅', n); } else { fail++; console.log('  ❌', n, NL + '     ได้  ' + JSON.stringify(got) + NL + '     ควร  ' + JSON.stringify(want)); } };
const c = (notes) => msgNote({ notes });
console.log(NL + '① โน้ตลูกค้าต้องขึ้นครบ');
t('บ้านสีเทา (U-0908-001)', c('บ้านสีเทาเลขที่ 33 อยู่ตรงข้าม พีพีเพลสที่ปักหมุดค่ะ'), 'บ้านสีเทาเลขที่ 33 อยู่ตรงข้าม พีพีเพลสที่ปักหมุดค่ะ');
t('ห้อง/ตึก', c('ห้อง 1402 ตึก 1'), 'ห้อง 1402 ตึก 1');
t('โน้ต + ป้ายยกเลิก (U-0908-004)', c('ตึก 3 ที่มีเสาธง [ยกเลิกโดย ต้องการสั่งใหม่ 2026-09-07] [เหตุผล: ต้องการสั่งใหม่]'), 'ตึก 3 ที่มีเสาธง');
t('โน้ต + เลื่อนวัน', c('ฝากในห้องนิติเท่านั้น (ห้องแอร์) [เลื่อนวันส่ง 2026-09-13 → 2026-09-14 โดย Suchada.p ( kif )]'), 'ฝากในห้องนิติเท่านั้น (ห้องแอร์)');
t('โน้ต + ป้ายของแพ้', c('บ้าน 88/76 [🩺 ไม่ทาน/ขอพิเศษ: ไม่กินยำ ส้มตำ ลาบ ปลาร้า กะปิ]'), 'บ้าน 88/76');
t('วงเล็บธรรมดาของลูกค้าไม่หาย', c('เผ็ดน้อย/ วางบนโต๊ะฝากอาหารตึกบี'), 'เผ็ดน้อย/ วางบนโต๊ะฝากอาหารตึกบี');
console.log(NL + '② ข้อความที่ระบบ/แอดมินเติม = ไม่ขึ้น');
t('รอบคอร์สเจ', c('คอร์ส คอร์สเจ 2569 — Early Bird (50 คนแรก) รอบ 3/3 — จ่ายรวมกับออเดอร์ U-1008-006'), '');
t('รอบ Meal Plan', c('Meal Plan HP รอบ 2/3 — จ่ายรวมกับออเดอร์ U-0916-011'), '');
t('รอบที่แอดมินเปิด', c('รอบ 3/3 ของคอร์ส · จ่ายรวมกับใบแรกแล้ว · เปิดใบโดย Suchada.p ( kif )'), '');
t('สั่งแทน', c('สั่งแทนโดย Suchada.p ( kif )'), '');
t('สั่งแทนชื่อสั้น', c('สั่งแทนโดย เพอรี่'), '');
t('เลื่อนรอบอย่างเดียว', c('[ลูกค้าเลื่อนรอบ 2026-09-18→2026-09-21]'), '');
t('ว่าง', c(null), '');
console.log(NL + '③ ขึ้นทั้ง 2 มุมมอง');
t('ตาราง', H.indexOf('+msgNoteHtml(o)+"</td>"') >= 0, true);
t('การ์ด', H.indexOf('h+=msgNoteHtml(o);') >= 0, true);
console.log(NL + 'ผ่าน ' + ok + ' · ตก ' + fail);
if (fail) process.exit(1);
